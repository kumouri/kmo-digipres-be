# CLAUDE.md — kmo-digipres-be

This file provides guidance to Claude Code when working with code in this repository.

## Repository Overview

`kmo-digipres-be` is the backend for the **KMOSF CRM** — a custom multi-tenant CRM / back-office platform built for KMO Solutions Foundry LLC. The "digipres" name and `com.kumouri` Java package are historical; treat any "digipres" / "kumouri" references as synonyms for the KMOSF CRM — not a separate product.

This is a **near-complete CRM / back-office platform** with multi-tenant scoping, auth, contacts, companies, deals, activities, billing, quotes + PDF, invoices + payments, product catalog, S3 storage, Twilio SMS, Postmark transactional email, inbox, email sequences, workflow automation, custom field definitions, module registry, portal auth, AI assist, RAG retrieval, lead scoring v2, GDPR compliance, reporting, service hub, home-services vertical, restaurant-light module, salon/spa module, CSV imports, public widget endpoints, the **project-delivery vertical (Phase C: projects / milestones / tasks)**, and the **time-and-expenses vertical (Phase D)**. For the full architectural overview and roadmap, see `docs/design/01-architecture-audit-and-crm-roadmap.md` (untracked — present in working tree; do not delete or commit without explicit instruction).

**Phase C — project-delivery vertical (SHIPPED).** `model.project` = `Project` + `Milestone` + `Task` (all `TenantScoped` + `Auditable`; `Project` also `CustomFieldHost`). One-click Deal→Project via `POST /projects/from-deal/{dealId}` — an explicit endpoint (mirrors `InvoiceService.createFromQuote`; **not** an event subscriber), idempotent through the explicit `existsByTenantIdAndDealId` boolean branch (201 first / 200 repeat); `DealCrudService`/`PipelineStage` are untouched. A `Milestone` with `triggersInvoiceOnComplete=true` spawns a **DRAFT** invoice through the existing `InvoiceService.create` (so no `INVOICE_FINALIZED`/QBO push), idempotent via the explicit `spawnedInvoiceId != null` guard; optional per-`Project` `autoFinalizeMilestoneInvoices` then transitions DRAFT→SENT. Project codes `PRJ-{year}-{seq:03}` come from `ProjectCodeGenerator` (atomic Mongo `$inc` upsert on `project_code_counters` keyed `tenantId:year`; per-(tenant,year); January reset is automatic via the year-keyed `_id`; the `Project.tenant_code_idx` unique compound is the defense-in-depth backstop). Error range **3400–3499** (`GlobalErrorHandler` Javadoc table). `@IdempotentRoute` on the 2 side-effecting POSTs only. Reactive rule honored: `switchIfEmpty` is used **only** for genuine not-found errors — the two idempotency seams use explicit boolean branches, never `switchIfEmpty(create)`.

**Phase D — time-and-expenses vertical (SHIPPED).** `model.timetracking` = `TimeEntry` + `Expense` (both `TenantScoped` + `Auditable`; neither `CustomFieldHost`). Package layout: `model/timetracking`, `service/timetracking`, `controller/timetracking`, `repository/timetracking` — symmetric with Phase C's `model.project` layout.

Key design decisions (D-D1 through D-D14, plan §3):

- **TimeEntry** (`@Document("time_entries")`): the highest-write entity; mandatory leading compound index `(tenantId, userId, startedAt desc)` = `tenant_user_started_idx`. Running timer ⇔ `endedAt == null, durationSeconds == 0`; stopped entry ⇔ `endedAt != null, durationSeconds` persisted (authoritative for billing). **Manual entry is first-class (Decision D9)** — `source=MANUAL` is the default; `source=TIMER` records provenance only. `BillingStatus {UNBILLED, INVOICED}` is the invoice idempotency dimension.
- **Midnight-split (D-D3 — the headline subtlety):** When a timer session is stopped (or a manual entry is saved) and `[startedAt, endedAt)` spans one or more **local-day** boundaries, `TimeSplitService` materializes N `TimeEntry` rows — one per local calendar day. Zone precedence: request `X-Zone-Id` header / `zoneId` param → `kmosf.timetracking.default-zone` → `ZoneOffset.UTC`. Same-local-day session → **one row, `splitGroupId == null`** (the overwhelmingly common case). Mon → Tue → **two rows**, shared non-null `splitGroupId`, half-open `[start, end)` boundary (the boundary instant belongs to the earlier day; no zero-length trailing row). > 24h → three rows. A still-running timer is **never** auto-split — only at stop/manual-save. `TimeSplitService` is pure/stateless/total.
- **Expense** (`@Document("expenses")`): `ApprovalStatus {PENDING, APPROVED, REJECTED}` workflow; only `APPROVED + billable + UNBILLED` expenses are invoice-eligible. `receiptAttachmentId` is a denormalized fast-path pointer (canonical link = `("EXPENSE", expenseId)` via the existing `Attachment` infra — zero new file-storage code, D-D12). `BillingStatus` reuses `TimeEntry.BillingStatus`.
- **Invoice-from-time (D-D6):** `TimeEntryService.createInvoiceFromTime` → existing `InvoiceService.create` → **DRAFT** (no `INVOICE_FINALIZED`, no QBO). Idempotent via `billingStatus == UNBILLED` filter + explicit `if (candidates.isEmpty())` guard (never `switchIfEmpty`). Split-aware aggregation (D-D6a): entries sharing a `splitGroupId` are summed into one `LineItem`. Per-entry `rateAmount` captured at log time; request-level `defaultRateAmount` fallback; 3521 if neither is set.
- **Invoice-from-expenses (D-D8):** `ExpenseService.createInvoiceFromExpenses` → same `InvoiceService.create` DRAFT seam. Markup: `amount × (1 + markup/100)` applied to `LineItem.unitPrice`. Same idempotency pattern.
- **Approval RBAC (D-D10):** `RoleGuard.requireRole("ADMIN")` on `POST /expenses/{id}/approve`, `POST /expenses/{id}/reject`, and both DELETE endpoints. Illegal transitions validated against an explicit `Set<String>` (the `MilestoneService.ILLEGAL_TRANSITIONS` pattern).
- **`@IdempotentRoute`** on: `POST /time-entries/timer/stop`, `POST /time-entries/invoice-from-time`, `POST /expenses/invoice-from-expenses` (the three side-effecting POSTs with external effects — D-D9).
- **`@ConditionalOnProperty(prefix="kmosf.modules.timetracking", name="enabled", matchIfMissing=true)`** on both controllers.
- **Receipt flow:** existing `/attachments/presign` → S3 PUT → `POST /attachments {subjectType:"EXPENSE", subjectId:<id>}` — zero new file-storage endpoints. `Attachment.subjectType` is a free-form String (verified `Attachment.java:42-46`).
- **§9 invariant (carry-forward from Phase C):** `switchIfEmpty` is used **only** for genuine entity-not-found errors (3500/3511). Timer start/stop guards and both invoice-from-X guards are explicit boolean branches — never `switchIfEmpty(create/stop/invoice)`.
- **Error range 3500–3599** (`GlobalErrorHandler` Javadoc table). TimeEntry 3500-3507; invoice-from-time 3520-3522; Expense 3511-3517; invoice-from-expenses 3530-3532.
- **Domain events** (advisory, `DomainEventType` Phase-D block): `TIME_ENTRY_LOGGED`, `TIMER_STARTED`, `TIMER_STOPPED`, `TIME_INVOICED`, `EXPENSE_SUBMITTED`, `EXPENSE_APPROVED`, `EXPENSE_REJECTED`, `EXPENSE_INVOICED`. None drive core mutations.
- **FE PR** (`back-office-kmo-digipres-phase-D-time-and-expenses` in `kmo-digipres-fe`) is the next step after that BE PR is merged — gated on the committed `docs/api/openapi.json` (HANDOFF GATE, D-D13).

**Phase E — recurring-billing + Stripe money-rails vertical (SHIPPED — BE; escalated `tenant_number_idx` blocker RESOLVED via a user-authorized §7 override — see the ✓ RESOLVED bullet).** `model.recurring` = `RecurringInvoice` (`TenantScoped` + `Auditable`, NOT `CustomFieldHost`) + `RecurringInvoiceOccurrence` (the idempotency ledger — `TenantScoped`, **NOT `Auditable`**, the `IdempotencyKey` rationale). Package layout `model/recurring`, `service/recurring`, `controller/recurring`, `repository/recurring`; the Quartz job + `InvoiceNumberIndexInitializer` live in `scheduling` next to `QuartzBootstrap`; finalize-time invoice numbering is `service/billing/InvoiceNumberGenerator`. Money-critical (Opus-implemented; separately Opus-validated).

Key design decisions (E-D1 through E-D14, plan §3):

- **RecurringInvoice spawn idempotency (E-D3 — the money core):** the Quartz `RecurringInvoiceSpawnJob` (`@DisallowConcurrentExecution`; `runDueOnce().block()` on the Quartz worker, **never the Netty event loop** — §9 item 2; `RecurringInvoiceJobScheduler` durable `ApplicationReadyEvent` registration, `@ConditionalOnProperty(kmosf.recurring-invoice.spawn-job.enabled, matchIfMissing=true)`) materializes **one DRAFT `Invoice` per cadence period via the existing `InvoiceService.create` seam** (the Phase-C/D DRAFT seam — invoicing NOT reinvented). Idempotency = **explicit-boolean occurrence probe** (`findBy…PeriodKey(...).map(e->true).defaultIfEmpty(false).flatMap(seen -> seen ? Mono.empty() : doSpawn)`) — **NEVER `switchIfEmpty(doSpawn)`** — over a **ledger-insert-FIRST** `doSpawn` (`RecurringInvoiceOccurrence.save` with unique `tenant_recurring_period_idx` BEFORE `invoiceService.create`; concurrent-fire `DuplicateKeyException` ⇒ `Mono.empty()` = zero invoice/double-bill/orphan). A **compensating delete** removes the just-inserted ledger row if a post-insert step fails (deferred, never lost, never double-billed). Bounded catch-up (`kmosf.recurring-invoice.max-catchup`, default 12, one invoice per missed period). RRULE via the existing `Rfc5545RecurringSchedule` (malformed → reused **1300**). ical4j `getDates` is **second-granularity** `from`-inclusive → cursor steps use a 1-second `OCCURRENCE_STEP` (a `plusMillis(1)` would freeze the cursor on the spent period). `autoFinalize=true` ⇒ spawned invoice DRAFT→SENT (fires `INVOICE_FINALIZED` → the shipped QBO sync); default `false` = stays DRAFT.
- **✓ RESOLVED — the escalated `tenant_number_idx` blocker (USER-AUTHORIZED §7 OVERRIDE; was: multi-period catch-up blocked).** The pre-existing **non-sparse non-partial** unique `Invoice.tenant_number_idx {tenantId,invoiceNumber}` rejected a 2nd `invoiceNumber==null` invoice per tenant (`E11000`), blocking recurring multi-period catch-up (and a latent defect for the Phase-C milestone-spawn / Phase-D time-and-expense-spawn unnumbered-DRAFT paths). The prior Opus implementer correctly STOPPED + escalated rather than improvising around plan §7 (which declared numbering a non-goal AND forbade an `Invoice` index/migration). **The user then authorized the combined correct fix, explicitly overriding plan §7's numbering non-goal — recorded here as an authorized scope change, not a violation:** (1) **finalize-time numbering** — `service/billing/InvoiceNumberGenerator` issues `INV-{YYYY}-{NNNN}` (per-(tenant,year), gapless by issue, January reset) via an atomic Mongo `$inc` upsert on `invoice_number_counters` keyed `tenantId:year` (cloned from `ProjectCodeGenerator` exactly); `InvoiceService` assigns it at the **first DRAFT→issued transition** (SENT/PARTIALLY_PAID/PAID/OVERDUE — `setStatus`, `createFromQuote`, and the payment auto-advance in `refreshInvoiceStatus`) **only when `invoiceNumber==null` (explicit-boolean idempotency — a re-finalize / VOID→SENT / DRAFT→PAID never regenerates; VOIDED is never numbered)**; `create`/`recordPayment`/`recomputeTotalsFromLines`/`maybePublishFinalized` semantics are otherwise untouched (surgical — number-assignment at the DRAFT-exit edge only); (2) **partial-unique `tenant_number_idx`** — the `@CompoundIndex` is removed from `Invoice.java` and the index is owned end-to-end by an idempotent `scheduling/InvoiceNumberIndexInitializer` (`@EventListener(ApplicationReadyEvent.class)`, the `QuartzBootstrap` pattern) that drops a pre-existing non-partial `tenant_number_idx` and creates a partial-unique one (`partialFilterExpression {invoiceNumber:{$type:"string"}}` — the MongoDB-legal "present & non-null"; safe + idempotent on a fresh Testcontainers DB AND on a DB carrying the old index). DRAFTs stay `invoiceNumber==null`; uniqueness holds only for issued/numbered invoices; many null-numbered DRAFTs per tenant are now legal. Defensive code `3640` (number-gen failed after retry — mirrors the Phase-C `3433`); `saveWithNumberRetry` adds the one-retry `DuplicateKeyException` backstop (the `ProjectService.generateCodeAndSave` C-D3 pattern). The 2 formerly-`@Disabled` AC-E3 full-catch-up specs in `RecurringInvoiceSpawnRestartIT` are **re-enabled and green**; the money invariants (ledger-first + compensating delete: zero double-bill/orphan/loss-after-success/merged-lump) still hold. A subsequent intermittent flake in those specs was **conclusively root-caused as a TEST-isolation artifact** (cross-tenant `runDueOnce()` + Spring-context-cached sibling contexts sharing ONE Testcontainers Mongo) — **NOT** a `RecurringInvoiceSpawnService` over-spawn/double-bill bug (proven: ≥5 green isolated runs; every failing run's ledger had distinct periodKeys 1:1 to invoices — zero double-bill). Test-only money-safe fixes: fixed `@Primary Clock`, foreign-tick-immune ledger-money-invariant assertions, global test spawn-job disable; zero production change. **Deeper test-isolation hardening — RESOLVED (post-Phase-E follow-up, PR #58 merged to `main`):** the E.13 test-yml `spawn-job.enabled=false` was found **shadowed** (Spring Boot loads main `application.properties` at higher precedence than test `application.yml`); the effective fix is a profile-specific `src/test/resources/application-test.properties` (profile `test`) that disables Quartz auto-startup in tests so no cached context's scheduler can tick `runDueOnce()` cross-tenant against the shared Testcontainers Mongo (`QuartzMongoJobStoreIT` is the sole opt-in) — still zero production/money change. Detail: `docs/PHASE-PROGRESS.md` (ESCALATED BLOCKER → RESOLVED + the catch-up root-cause section + the post-Phase-E test-isolation section).
- **Quartz Mongo JobStore (E-D5 — RAM-FALLBACK ACTIVE):** `io.fluidsonic.mirror:quartz-mongodb:2.2.0-rc2` **resolves** and the Mongo store **loads**, but it was compiled against Quartz 2.3.2 and Spring Boot 3.5.6 manages Quartz 2.5.0 (removed `JobDetail.isConcurrentExectionDisallowed()`) → `NoSuchMethodError` in `com.novemberain.quartz.mongodb.LockManager` on every trigger fire. Per plan §9 item 1 / E-D5 the **RAM-store fallback** is active (`kmosf.quartz.store=memory`; the dep is intentionally NOT in `build.gradle`; `QuartzConfig`'s mongo branch + the flag stay wired for a future Quartz-2.5-compatible release). **Money-durability does NOT depend on the JobStore** — it lives in the `RecurringInvoiceOccurrence` Mongo ledger + `nextRunAt` cursor (the `ServiceAgreementSchedulerService` model). `QuartzMongoJobStoreIT` documents the fallback; `RecurringInvoiceSpawnRestartIT` is the durability proof.
- **`@IdempotentRoute` adoption set (E-D6, deferred from Phase A):** `POST /invoices`, `POST /invoices/{id}/payments` (added in place on `InvoiceController`), `POST /invoices/{id}/stripe-checkout`, `POST /recurring-invoices/{id}/spawn-now`. **NOT** `from-quote`, `status`, or the Stripe webhook (Stripe sends no `Idempotency-Key`; webhook idempotency is event-id-based). `spawn-now` returns the refreshed `RecurringInvoice` (a `Mono<Void>` `@IdempotentRoute` handler hangs — the response-tee needs a body).
- **Stripe webhook (E-D7 — closed the biggest pre-existing money gap):** `StripeWebhookService` extended **in place** — idempotent on the Stripe **event id** (`StripeWebhookEvent` system ledger, unique `tenant_event_idx`, NOT `Auditable`) via the same explicit-boolean + ledger-insert-FIRST pattern; a duplicate delivery → **200 no-op** (NOT 409 — Stripe retries harder). Reuses `StripeSignatureVerifier` **verbatim** (zero new HMAC) + `InvoiceService.recordPayment` unchanged; then emits `INVOICE_PAID {invoiceId,contactId,amount,currency,source:"stripe"}` (mirrors `SquarePosService.emitInvoicePaid`; `LoyaltyAccrualService` accrues on Stripe too). New code `3610` (event missing id).
- **Stripe Checkout (E-D9):** `StripeCheckoutService` — raw `WebClient` (NO `com.stripe:stripe-java`) against `StripeProperties.apiBaseUrl` (`kmosf.stripe.api-base-url`, default `https://api.stripe.com`, **configurable → WireMock in every test**), Resilience4j `stripe-checkout` breaker, session/payment-link form carries `metadata[kmosf_invoice_id]`. Codes `3620` (apiKey missing, 412) / `3621` (creation failed, 502). `StripeProperties` registered via `StripeConfig` (`@EnableConfigurationProperties`).
- **Accounting-push (E-D10):** `AccountingPushService` + `POST /invoices/{id}/accounting-push` (ADMIN) — reaches the **already-shipped** `INVOICE_FINALIZED → QuickBooksInvoiceSync` seam (DRAFT→SENT via unchanged `setStatus`, or re-publish `INVOICE_FINALIZED` if already SENT). **`QuickBooksInvoiceSync` is NOT modified** (verified empty diff vs `main`); idempotent via `externalRefs["quickbooks"]`. Code `3630` (not-eligible/VOIDED).
- **`paymentTerms` (E-D8):** additive nullable `Invoice.PaymentTerms {DUE_ON_RECEIPT,NET_7,NET_15,NET_30,NET_45,NET_60}` enum + nullable field (no `@Builder.Default`; null ⇔ unspecified ⇒ treated as NET_30 only where `dueAt` derived) + pure static `Invoice.deriveDueAt`. **No `Invoice.Status`/index/migration change** (legacy invoices deserialize `paymentTerms=null`).
- **Module gate:** `RecurringInvoiceController` + the Stripe-checkout/accounting-push endpoints (split into `InvoiceBillingStripeController`) are `@ConditionalOnProperty(prefix="kmosf.modules.billing-stripe", name="enabled", matchIfMissing=true)`. Core `InvoiceController` stays always-on (a minor reconciliation of E-D9's gate with E-D12's "extended in place"). The Stripe webhook path is the existing `/public/**` permitAll, NOT gated.
- **§9 invariant (carry-forward):** `switchIfEmpty` in `service/recurring/*` + `integration/stripe/*` is used **only** for genuine entity-not-found (RecurringInvoice 3605 ×2; pre-existing Stripe-not-connected 2510). The recurring-occurrence and Stripe-event-id idempotency seams are explicit-boolean — never `switchIfEmpty(spawn/process)`.
- **Error range 3600–3699** (`GlobalErrorHandler` Javadoc). RecurringInvoice 3601-3607; Stripe webhook 3610; Stripe checkout 3620/3621; accounting-push 3630; invoice-number-gen-failed (blocker resolution) 3640. Reused not re-allocated: 1300/2300/2510/2511/2800-2802/3100/3101.
- **Domain events** (advisory, `DomainEventType` Phase-E block): `RECURRING_INVOICE_CREATED/SPAWNED/PAUSED/ENDED`, `STRIPE_CHECKOUT_CREATED`. The Stripe webhook **reuses the existing `INVOICE_PAID`** (NOT a new event). None drive core mutations.
- **HARD no-live-money boundary (§7):** every Stripe/QBO interaction is WireMock/sandbox; `kmosf.stripe.api-base-url` configurable so no test touches `api.stripe.com`; webhook signatures verified with a test signing secret; apiKey a sandbox `sk_test_`-shaped fake. **No host hardcoded, no live key in any fixture.** Wiring a live Stripe key / initiating a real charge is a separate human action — never the implementation loop.
- **Phase F** (`Contract`/`ContractTemplate`, Documenso) is the contracts vertical — see the Phase F SHIPPED section below. The client-facing Stripe-checkout UI + portal payment surface is **Phase G** (E-D13: Phase E is **BE-only**; the recurring/Stripe FE is deferred to Phase G — no FE PR for Phase E).

**Phase F — contracts vertical (SHIPPED — BE-only; FE deferred, F-D14).** `model.contract` = `Contract` + `ContractTemplate` (both `TenantScoped` + `Auditable`) + `DocumensoWebhookEvent` (system-ledger; `TenantScoped`, **NOT `Auditable`** — the idempotency-ledger rationale, mirrors `RecurringInvoiceOccurrence`). Package layout: `model/contract`, `service/contract`, `controller/contract`, `repository/contract`; the Documenso integration lives in `integration/documenso`; the index initializer in `scheduling`. Money-legal (signed-PDF + exactly-once SOW→WON+Project path; Opus-implemented; Opus-validated).

Key design decisions (F-D1 through F-D15, plan §3):

- **Contract lifecycle** `DRAFT → SENT → SIGNED` (terminal-happy); `DRAFT|SENT → VOIDED` (terminal). `SIGNED` is immutable via the API — only the verified Documenso webhook moves `SENT→SIGNED`. **Amendment model:** an amendment is a NEW `Contract` with `parentContractId` pointing at the contract it amends; no in-place mutation of a signed contract ever occurs.
- **ContractTemplate** (`@Document("contract_templates")`): `kind ∈ {MSA, SOW, NDA, OTHER}`, `active` flag, `bodyTemplate` (Mustache), `defaultTitle`. `ContractTemplateService` owns CRUD; only one template per (tenant, kind) may be `active=true` (3705 if missing). `ContractService` resolves the active template at send-time (not at create-time — a DRAFT may reference a now-inactive template id and rendering still succeeds).
- **ContractPdfService (F-D4):** jmustache renders `bodyTemplate` → OpenPDF (PDFBox via existing `QuotePdfService` pattern) on `Schedulers.boundedElastic()` (blocking PDF I/O off the Netty event loop). `GET /contracts/{id}/pdf` renders fresh from the template each time (no stored pre-sign ref returned via this endpoint). `renderedPdfStorageRef` (pre-sign, S3) ≠ `signedPdfStorageRef` (post-sign, S3) — distinct fields, never overwritten. PDF-generation failure uses error code **3799** (plan F-D11 did not allocate a dedicated code; `3799` is the in-range last-bucket overflow choice in the reserved 3700-3799 block, traceable — recorded deviation).
- **Quote-ACCEPTED→SOW explicit-boolean idempotency (F-D6):** `ContractService.spawnFromQuote` is the explicit idempotent endpoint — NOT an event subscriber. Probe: `contracts.existsByTenantIdAndQuoteIdAndTemplateId(...).map(seen → seen ? returnExisting : doCreate)`. Mirrors `ProjectService.convertFromDeal` (C-D4); avoids the `switchIfEmpty(create)` double-create trap. `@IdempotentRoute` on both `POST /contracts/{id}/send` and `POST /contracts/quotes/{quoteId}/spawn-contract`.
- **ContractNumberGenerator (F-D5 + F-D10 correction):** `CTR-{YYYY}-{NNNN}` — per-(tenant, year), gapless by issue, January reset automatic via year-keyed `_id` (atomic `$inc` upsert on `contract_number_counters`). Verbatim clone of `InvoiceNumberGenerator`. Numbers assigned at the first DRAFT→SENT edge; DRAFT contracts stay `contractNumber == null`. **F-D10 (necessary correction — the E.11 lesson):** the plan said `@CompoundIndex` could declare the partial-unique `contracts.tenant_number_idx`. Spring Data `@CompoundIndex` **cannot** express `partialFilterExpression` — this is the literal root cause from Phase E. Applied the proven `scheduling/ContractNumberIndexInitializer` pattern (mirrors `InvoiceNumberIndexInitializer`): a key-only `@CompoundIndex` on `Contract.java` + the initializer owns the drop-and-recreate partial-unique index (`partialFilterExpression {contractNumber:{$type:"string"}}`). This is the only working approach. Defensive code `3708` (counter-empty after retry).
- **DocumensoClient (F-D7):** raw `WebClient` (no Documenso Java SDK) + Resilience4j `documenso` circuit breaker (mirrors `stripe-checkout`/`quickbooks-invoice`). Base-URL precedence: `IntegrationConnection.config["apiBaseUrl"]` → `DocumensoProperties.apiBaseUrl`; the default is `https://documenso.internal.invalid` (non-routable `.invalid` TLD — no host hardcoded, no live token in any fixture). `send()` uses error code `2510` for "Documenso not connected" (the established cross-integration not-connected convention — `StripeWebhookService` precedent). Note: the F.6 webhook path uses `3712` for the same not-connected condition (plan F-D7); send-path=2510 vs webhook=3712 is a documented minor inconsistency — 2510 is the cross-integration convention, 3712 is plan-specified for the webhook. Validator-recorded; acceptable.
- **HMAC-verified Documenso webhook (F-D8 — the legal-signature headline path):** `DocumensoWebhookService` mirrors `StripeWebhookService` exactly: constant-time HMAC-SHA256 hex verify (`DocumensoSignatureVerifier`, `X-Documenso-Signature` header) → **ledger-insert-FIRST** (`DocumensoWebhookEvent.save` with unique `tenant_event_idx` BEFORE dispatch; concurrent-duplicate `DuplicateKeyException` ⇒ `Mono.empty()` = 200 no-op) → explicit-boolean event-id idempotency probe → dispatch. Signature failure → `3710,401`. Duplicate delivery → `200 no-op` (NOT 409). Tenant resolved from URL path (`/{tenantId}/webhook`) → `IntegrationConnection` lookup — **never from the webhook payload**. Synthetic `TenantContext(tenantId, null, Set.of("INTEGRATION_DOCUMENSO"))` via `contextWrite(TenantContextHolder.write(ctx))` (the `StripeWebhookService`/`QuickBooksInvoiceSync` precedent). The webhook controller carries **no `@ConditionalOnProperty`** — it lives under `/public/**` and is always active.
- **Documenso-payload assumption + adapter boundary (F-D7):** `DocumensoEventAdapter.parse` is the **only** place payload fields (`id|eventId`, `event|type`, `payload.{documentId,downloadUrl?}`) are assumed. `DocumensoSignatureVerifier` is the **only** place the digest scheme lives. The controller `@RequestHeader("X-Documenso-Signature")` is the **only** place the header name lives. Correcting against a real Documenso deployment = a one-method change in each of those three files only (≤3 files). The WireMock stub in `DocumensoWebhookSignedIT`/`DocumensoSendWireMockIT` is the single contract coded-to.
- **Signed PDF storage + legal-integrity rule:** `DOCUMENT_SIGNED` webhook flow: verify → load event-id idempotency → correlate `Contract` by `documensoDocumentId` (`3716` if absent) → download signed bytes → `FileStorageService.putBytes` (`3717` on failure) → set `signedAt` + `signedPdfStorageRef` + `status=SIGNED` → save → publish `CONTRACT_SIGNED`. **Legal-integrity invariant:** store Documenso's exact bytes, never re-render. `renderedPdfStorageRef` (pre-sign render) ≠ `signedPdfStorageRef` (post-sign bytes from Documenso) — distinct fields, never overwritten.
- **Exactly-once SOW→WON+Project (F-D9):** iff `kind==SOW && dealId!=null && !promotedDealToWon` → reuse the **existing** `DealCrudService.moveStage(dealId, WON, null)` then `ProjectService.convertFromDeal(dealId)` — no parallel path, `DealCrudService`/`ProjectService` are unmodified (empty diff vs `main`). Doubly idempotent: the `!promotedDealToWon` explicit-boolean gate on `Contract` + `ProjectService.convertFromDeal`'s own `existsByTenantIdAndDealId` gate.
- **Module gate:** `kmosf.modules.contracts.enabled` (`matchIfMissing=true`) on `ContractController` + `ContractTemplateController`. The Documenso webhook path (`/public/integrations/documenso/{tenantId}/webhook`) is NOT gated — always active under `/public/**` permitAll.
- **Error range 3700–3799** (`GlobalErrorHandler` Javadoc table). ContractTemplate 3701-3702; Contract 3703-3710; ContractNumberGenerator 3708; webhook 3710-3717; PDF-fail 3799. Reused not re-allocated: 2510 (Documenso-not-connected on the send path, cross-integration convention).
- **§7 no-live-Documenso hard boundary:** every Documenso interaction is WireMock/sandbox; `IntegrationConnection.config["apiBaseUrl"]` → `DocumensoProperties.apiBaseUrl` configurable → pointed at WireMock in all tests; no host hardcoded; apiToken a sandbox fake; webhook secret a test secret. A real Documenso deployment is a separate human action — never the implementation loop.
- **`auto-index-creation=false` test-only workaround:** `DocumensoSendWireMockIT` + `DocumensoWebhookSignedIT` use `@MockBean FileStorageService`, creating distinct Spring `ApplicationContext`s that share the Testcontainers Mongo. The second context's Spring Data auto-index-creation conflicts with `ContractNumberIndexInitializer`'s partial-unique index. Fix: `spring.data.mongodb.auto-index-creation=false` in `@TestPropertySource` for those two IT classes — test-only, zero production change. Production keeps auto-index-creation ON; `ContractEntityIndexIT` independently proves the partial-unique index auto-creates correctly. AC-F2 exactly-once is unaffected (the IT's re-delivery test is sequential; unique index is the concurrent backstop).
- **Domain events** (advisory, `DomainEventType` Phase-F block): `CONTRACT_CREATED`, `CONTRACT_SENT`, `CONTRACT_SIGNED`, `CONTRACT_VOIDED`, `CONTRACT_TEMPLATE_CREATED`, `CONTRACT_TEMPLATE_UPDATED`. None drive core mutations.
- **FE deferred (F-D14):** BE-only Phase F; the ContractTemplate-admin + generate-from-template wizard + minimal contract-status FE is a user opt-in post-BE-merge — no FE PR for Phase F.

**Phase G — portal expansion (SHIPPED — BE-only; client-portal FE deferred, G-D12).** The client-facing portal surface: read-mostly `/portal/me/**` endpoints letting an authenticated portal user (a `Contact` linked via `User.contactId`) see and act on **only their own tenant + contact/company** data — list/detail for invoices, quotes, projects, contracts and project files; pay an invoice via Stripe-hosted checkout; accept/decline a quote; magic-link email deep-links. New packages: `controller/portal/Portal{Quotes,Projects,Contracts,Files}Controller` (plus the extended `PortalInvoicesController`/`PortalAuthController`), `service/portal/{PortalOwnershipGuard,Portal{Quotes,Projects,Contracts}Service,PortalInvoiceViewService}`, `model/response/Portal{Quote,Project,Contract,File}Summary` + `PortalCheckoutResponse` projection records, portal-scoped finders on the `Invoice/Quote/Project/Contract` repos. Access control is the headline; Opus-validated (access-control + payment focus). No new `SecurityWebFilterChain`, no module gate on portal controllers — the existing portal chain + per-user `User.contactId` is the gate.

Key design decisions (G-D1 through G-D13, plan §3):

- **All portal endpoints under `/portal/me/**`, projection records out, never raw entities (G-D1).** New `controller/portal/Portal*Controller`s funnel through the **existing, unmodified** `PortalLinkedContactResolver` chokepoint and return `Portal*Summary`/`PortalCheckoutResponse` records that drop `tenantId`/staff-internal fields; no new security config; no `@ConditionalOnProperty` (the existing portal-controller precedent).
- **`PortalLinkedContactResolver` chokepoint + `PortalOwnershipGuard` (G-D2 — THE access-control invariant).** `PortalLinkedContactResolver.resolve()` (tenant from JWT, contact from `User.contactId`, tenant-scoped contact load — codes 1250/1251/1252) is the single chokepoint for every `/portal/me/**` data endpoint. `PortalOwnershipGuard.requireOwned{Invoice,Quote,Project,Contract}` composes on it for single-entity ops: tenant-scoped `findByTenantIdAndId(contact.tenantId, id)` then a `contactId==contact.id || companyId==contact.companyId` predicate. **Not-found and not-owned return the SAME `38xx`/404** — a deliberate no-enumeration-oracle confidentiality choice (a portal user cannot distinguish "exists but isn't yours" from "no such entity"). `isOwnedBy` is a pure static predicate that never matches `null==null`; `Project` ownership uses `primaryContactId`. `PortalOwnershipGuard` is a stateless `@Component` (no new resource-owning bean).
- **Portal pay = gate-before-checkout, reuse `StripeCheckoutService` unchanged (G-D3 — §9 #2, most-scrutinized).** `POST /portal/me/invoices/{id}/pay` (`@IdempotentRoute`): `requireOwnedInvoice(id)` resolves **first** and textually + reactively **precedes** `StripeCheckoutService.createCheckoutForInvoice(...)` (the Stripe call is inside the `.flatMap` after the gate). A non-owned/cross-tenant invoice errors `3801`/404 at the gate with **zero Stripe traffic** (`PortalInvoicePayWireMockIT`: rejected invoice → 3801 + WireMock recorded ZERO requests; same key replays the URL — exactly-once). `StripeCheckoutService` byte-unchanged vs `main`; WireMock/sandbox base URL only.
- **`INVOICE_VIEWED_BY_CLIENT` = advisory event + best-effort `Activity(NOTE)` (G-D4).** `GET /portal/me/invoices/{id}` emits the advisory `INVOICE_VIEWED_BY_CLIENT` event + an `Activity(type=NOTE)` row via `PortalInvoiceViewService` (the `PortalTicketsController` precedent). Both are fire-and-forget — `viewService.recordView(...).onErrorResume(e -> Mono.empty())`; a telemetry-write failure **must not fail the read**. `ActivityType` is unchanged (`NOTE` reused, NOT a new enum value).
- **Portal quote accept/decline = gate-then-`QuoteService.setStatus` unchanged (G-D5).** `POST /portal/me/quotes/{id}/{accept,decline}` → ownership gate → reuse the **unchanged** `QuoteService.setStatus` (SENT-only allow-list; `3805` if not SENT); advisory `PORTAL_QUOTE_ACCEPTED`/`PORTAL_QUOTE_DECLINED` events. **Portal-accept does NOT auto-spawn a SOW contract** — Phase F's `spawnFromQuote` stays an explicit staff action; Phase G does not re-introduce event-driven contract creation. `QuoteService`/`QuoteController`/`DealCrudService`/`ProjectService` empty diff vs `main`.
- **Contract Documenso deep-link + signed-PDF presign, no live Documenso (G-D6).** Read-only contract projections; the "Documenso deep-link" is a **derived URL surfacing the stored `documensoDocumentId`** — never a live Documenso call. `GET /portal/me/contracts/{id}/signed-pdf` returns a presigned download URL via the existing presign infra under the ownership gate (`3806` if not SIGNED); the raw `signedPdfStorageRef` is never exposed; `presignDownload`'s foreign-tenant `1311` is the storage backstop. Project files (`GET /portal/me/projects/{id}/files[/{fileId}/download]`) gate on project ownership then confirm `tenantId`+`subjectType="PROJECT"`+`subjectId` (same-404 `3807`); presign-URL only — no bytes stream through the BE.
- **Magic-link deep-link = existing `linkBaseUrl` + additive nullable `redirectTo` (G-D7).** `MagicLinkService.buildLink` is **byte-unchanged** (the existing `linkBaseUrl` already routes to `/portal/contracts/<id>` etc.). An additive **nullable** `redirectTo` is persisted at **request-time** on `MagicLinkToken` and echoed at redeem; the echoed redirect is **always** the persisted token value, never an attacker-supplied value at redeem-time (open-redirect-safe — the `MagicLinkRedeem` body has only `token`). Legacy tokens deserialize `redirectTo=null` ⇒ byte-identical behavior. `MagicLinkService.redeem()` return changed `Mono<User>`→`Mono<RedemptionResult(User, String redirectTo)>` (an internal-service-signature wrapper, NOT an HTTP-contract change); redeem single-use/expiry security (1242/1243/1244) unchanged.
- **Error range 3800–3899 (G-D8)** (`GlobalErrorHandler` Javadoc table): invoice-not-owned 3801, quote 3802, project 3803, contract 3804, quote-not-SENT 3805, contract-not-SIGNED 3806, project-file 3807. Reused not re-allocated: 1250-1252 (resolver), 1242/1243/1244 (magic-link), 2510/3620/3621/1310/1311/3100/3101 (reused-service surfaces).
- **Portal services own the contact/company branch dispatch (G-D9).** One list service per surface (the `PortalInvoicesService` precedent): `companyId != null` → the `…OrCompany` finder, else contactId-only (the `{companyId:null}` trap avoided). Every portal repo finder carries an **explicit `tenantId` AND a contact/company predicate** — the `TenantScopedReactiveMongoRepository` marker does NOT auto-scope derived finders.
- **No new resource-owning client (G-D10 — the F.2/F.11 lesson).** Phase G introduces no new SDK/HTTP-Netty client/pool/scheduler/thread-pool bean — it reuses `StripeCheckoutService`'s shared `WebClient.Builder` and the Phase-F-`Closeable` `S3FileStorageService`. Verified: none introduced.
- **Shard-safe ITs (G-D11 — the F.12 lesson).** Six new ITs in the existing `portal/` package mirror `PortalInvoicesIT` (cross-tenant + cross-contact) + `StripeCheckoutWireMockIT` (WireMock pay): **no `@MockBean`**, no per-IT distinct property source beyond the one WireMock base-URL `@DynamicPropertySource` the pay IT needs, self-clean `mongo.remove` `@BeforeEach`; no `application-test.properties`/`build.gradle` shard-logic change (Phase G is shard-neutral).
- **§7 HARD no-live-money / no-live-Documenso boundary.** The portal pay path reuses `StripeCheckoutService` against `kmosf.stripe.api-base-url` pointed at WireMock in every test with a sandbox `sk_test_`-shaped fake key — no host hardcoded, no live key, no real charge. The contract Documenso deep-link surfaces the stored id only — never a live Documenso call. **Reused subsystems empty diff vs `main`:** `PortalSecurityConfig`, `PortalLinkedContactResolver`, `QuoteService`, `QuoteController`, `StripeCheckoutService`, `FileStorageService`, `S3FileStorageService`, `DealCrudService`, `ProjectService`, `MagicLinkService.buildLink`. Wiring a live Stripe key / real Documenso is a separate human action — never the implementation loop.
- **§9 invariant (carry-forward):** `switchIfEmpty` in `service/portal/` + `controller/portal/` is used **only** for genuine entity-not-found (the resolver's 1251/1252, the guard's 3801-3804, files 3807, magic-link 1242). Phase G has no conditional-create idempotency seam (pay delegates to the existing `@IdempotentRoute` checkout; accept/decline to the unchanged `QuoteService.setStatus`) — never `switchIfEmpty(create/...)`.
- **Domain events** (advisory, `DomainEventType` Phase-G block): `INVOICE_VIEWED_BY_CLIENT` (`invoice.viewedByClient`), `PORTAL_QUOTE_ACCEPTED` (`portal.quoteAccepted`), `PORTAL_QUOTE_DECLINED` (`portal.quoteDeclined`). None drive core mutations.
- **FE deferred (G-D12/G-D13):** BE-only Phase G; a client-portal SPA is a user opt-in **strictly after** the BE PR merges + `docs/api/openapi.json` is committed on `main` (the BE→FE handoff gate), recommended as its own dedicated phase (greenfield app, not a thin `admin/<area>/*` add). The BE PR still regenerates+commits `docs/api/openapi.json` so a future portal-FE codegen has the Phase-G schemas. Docs in-PR (`CLAUDE.md` + `docs/api/openapi.json`); `.claude/*` local-only (gitignored); PR `gh pr merge --merge` (never squash).

**Phase H — external-integrations-glue (SHIPPED — BE-only).** Wires five external integration surfaces onto already-shipped infrastructure: Cal.com (booking source-of-truth webhook → `Meeting` projection), IMAP inbound poll-source, Postmark bounce/spam routing (in-place), Activepieces outbound-webhook-subscription seed, and opt-in portal Zitadel federation. No FE this phase. Opus-implemented; separate Opus validation (external-ingress trust + auth-regression focus).

Key design decisions (H-D1 through H-D11, plan §3):

- **Cal.com source-of-truth ADR (H-D2): `Meeting`/`BookingLink` are cached projections; `CalComWebhookService` MIRRORS `StripeWebhookService` verbatim.** Verify-before-effect (HMAC-SHA256 hex, `CalComSignatureVerifier`) → ledger-insert-FIRST under unique `tenant_event_idx` (`CalComWebhookEvent`) with `DuplicateKeyException→Mono.empty()` concurrent-safe backstop → explicit-boolean idempotency probe (NEVER `switchIfEmpty(process)`) → synthetic `TenantContext(tenantId, null, Set.of("INTEGRATION_CALCOM"))` via `contextWrite`. Tenant from URL path (`/public/integrations/calcom/{tenantId}/webhook`) — **never the payload**. Duplicate redelivery → 200 no-op. Additive-nullable `Meeting.calComBookingUid` (sparse index) is the projection key; `MeetingCrudService`/`BookingService` empty diff vs `main`. Cal.com sig scheme + payload shape isolated to `CalComSignatureVerifier` + `CalComEventAdapter` (F-D7 adapter-boundary — ≤3 files to correct against a real Cal.com deployment). Cal.com `apiBaseUrl` default `https://api.cal.invalid` (non-routable `.invalid`; configurable). Error codes: `3900` sig-invalid, `3901` not-connected, `3902` bad-body/missing-event-id, `3903` no-resolvable-contact (advisory-skip). Advisory domain events: `CALCOM_BOOKING_SYNCED`, `CALCOM_BOOKING_CANCELLED` (Phase-H `DomainEventType` block).
- **IMAP inbound poll-source (H-D3): new `ImapInboundPoller` feeds the UNCHANGED `InboundEmailService.ingest`.** Spring `@Scheduled`; `@ConditionalOnProperty(prefix="kmosf.imap.inbound", name="enabled", matchIfMissing=false)` — **default-OFF** (no live IMAP in CI or any default run). Per-cycle IMAP Store open/close on `Schedulers.boundedElastic()` (no long-lived connection bean). RFC-822 Message-ID explicit-boolean idempotency (skip already-seen message-IDs — never `switchIfEmpty(ingest)`). `ImapProperties` host default `imap.mail.invalid` (non-routable `.invalid`; configurable). `InboundEmailService` empty diff vs `main`. Error codes (internal, logged — never client-surfaced): `3910` connection/auth failure, `3911` unparseable message.
- **Postmark bounce/spam routing (H-D4): in-place additive extension of `PostmarkWebhookService`** — the existing Delivery/Open/Click byte-unchanged. Reuses `PostmarkAuthVerifier` verbatim (zero new HMAC) + existing `EMAIL_BOUNCED`/`EMAIL_SPAM` (no new event, no new controller). Idempotency: probe-then-act on `EmailEngagement` (`findFirstByTenantIdAndMessageIdAndEvent` → explicit-boolean → save/publish/flag). Additive-nullable `Contact.emailDeliverability` + `EmailDeliverabilityStatus` enum. **Orchestrator flag (H.8 Opus validator adjudicates):** in-place posture is sequentially idempotent but NOT concurrent-safe (no ledger-insert-FIRST backstop) — acceptable as in-place extension of the shipped Postmark webhook posture (advisory/non-money effects; E-D7 precedent; the validator must explicitly rule).
- **Activepieces per-tenant outbound-webhook-subscription seed (H-D5): `ActivepiecesSubscriptionSeeder` over the UNCHANGED `WebhookDeliveryService`.** Idempotent per (tenant, url, eventType) via explicit-boolean exists-probe (NEVER `switchIfEmpty(create)`). Stored secret = **read-only capability token** (R8 — no admin-scope token stored anywhere). `POST /api/v1/integrations/activepieces/seed` with `@IdempotentRoute` + `@ConditionalOnProperty(prefix="kmosf.modules.activepieces", name="enabled", matchIfMissing=true)`. `WebhookDeliveryService`/`WebhookSubscription` empty diff vs `main`. No live Activepieces (seed persists `WebhookSubscription` row only; no live HTTP at seed time). Error codes: `3920` invalid target URL, `3921` module disabled.
- **Opt-in portal Zitadel federation (H-D6): per-tenant `Tenant.zitadelOrgId` gate; server-side `oauth2Login` additive only.** Reuses A2 `ZitadelClaimTenantResolver`/JIT-provisioner verbatim (zero new identity logic). `PortalSecurityConfig` additive-only (OAuthRegistrationsSummary 2→3 params; SecurityWebFilterChain bean byte-identical). **Non-opted tenant local magic-link/passkey path byte-identical** (proven by existing portal ITs + `MagicLinkRedeemIT`/`PortalChainIT` green). Opt-in gate: `tenant.zitadelOrgId == null → 3930` before any session mint (fail-closed). `PortalZitadelSuccessHandler` passes OIDC claims to `ZitadelClaimTenantResolver` via `buildJwtFromOidcUser` adapter (H.8 validator focus: confirm claim names `urn:zitadel:iam:org:id`/roles/email pass through unchanged). Empty-default Zitadel config (`KMOSF_OAUTH_ZITADEL_PORTAL_ISSUER` env, blank default; no live Zitadel host/secret in any default or test run). Error codes: `3930` federation not enabled for tenant, `3931` callback state/nonce invalid. Reused: `3300-3303` (A2 Zitadel runtime).
- **Error range 3900-3999 (H-D8).** Cal.com: 3900-3903; IMAP (internal): 3910-3911; Activepieces: 3920-3921; portal-Zitadel: 3930-3931; reserved 3940-3999. Reused (NOT re-allocated): 2510 (cross-integration provider-not-connected), 3100/3101 (idempotency middleware), 3300-3303 (A2 Zitadel runtime).
- **No-live-external boundary (§7 — the headline hard line).** Cal.com/IMAP/Mailcow/Activepieces/Postmark/Zitadel = WireMock/sandbox/stub/`.invalid`-default throughout; configurable base URLs; default-OFF pollers; no host hardcoded; no live token/charge/deployment anywhere in the implementation loop. Wiring a live external system is a separate human action — never the loop.
- **BE-only; no FE this phase (H-D11).** Track-1 loop **PAUSES after Phase H** — Phase I (per-tenant deployment automation) is deferred per ultraplan D13 until the first paying client is funded. Do not start Phase I.

**Phase 1 — NMM voicemail-to-lead pipeline (SHIPPED — BE-only).** The first slice of the NMM AI intake layer (plan `i-had-an-idea-ticklish-rivest.md` §2/§3/§5/§6 — Track-3 E1, "AI-leveraged custom-build agency"). A missed-call voicemail pipeline that is a **faithful structural mirror of the shipped Cal.com webhook (Phase H — `CalComWebhookService`)**. New packages: `integration/twilio/voice/` (validator + transcription seam + extraction service + the orchestrating `TwilioVoicemailService`), `controller/integration/Twilio{Voice,Voicemail}Controller`, `model/integration/TwilioVoicemailEvent` (ledger), `repository/twilio/TwilioVoicemailEventRepository`. BE-only; no FE this phase. No live external services (the §7 hard boundary — WireMock/mock/sandbox throughout).

Key design decisions:

- **Two `/public/**` webhook endpoints, tenant-from-path.** `POST /public/integrations/twilio/{tenantId}/voice` returns **TwiML** (`Say` greeting + `Record transcribe="true" transcribeCallback=".../voicemail" maxLength playBeep`); `POST /public/integrations/twilio/{tenantId}/voicemail` is the transcription callback (Twilio posts `application/x-www-form-urlencoded` params `CallSid, RecordingSid, RecordingUrl, From, To, TranscriptionText, TranscriptionStatus`). Tenant is resolved from the URL **path only** — never the payload (4003 on a bad UUID). Both reachable via the existing `/public/**` permitAll rule.
- **Verify-before-effect (the §9 #1 external-ingress invariant — mirrors `CalComWebhookService`).** `TwilioVoicemailService.verifiedConnection` resolves the tenant's `IntegrationConnection(provider="twilio")` → reads `secrets.authToken` → verifies the `X-Twilio-Signature` via the new **`TwilioRequestValidator`** (the adapter boundary, like `CalComSignatureVerifier`/`StripeSignatureVerifier`; Twilio's scheme = `base64(HMAC-SHA1(authToken, fullRequestUrl + concat(sorted POST key+value)))`, constant-time compare, the ONLY place the scheme lives) **before any effect**. Not-connected/missing-authToken → `4001`/404; signature-invalid → `4000`/401. The full signed URL is reconstructed proxy-aware (`X-Forwarded-Proto`/`X-Forwarded-Host` → request URI) so it matches the public Cloudflare/Caddy URL Twilio signed.
- **Idempotency: explicit-boolean `CallSid` probe + ledger-insert-FIRST (NEVER `switchIfEmpty(process)`).** `voicemailEvents.findByTenantIdAndCallSid(...).map(e->true).defaultIfEmpty(false).flatMap(seen -> seen ? Mono.empty() : processAndRecord)`. `processAndRecord` saves the `TwilioVoicemailEvent` (system ledger — `TenantScoped`, **NOT `Auditable`**, unique compound `tenant_callsid_idx {tenantId,callSid}`) **FIRST**, with `onErrorResume(DuplicateKeyException.class → Mono.empty())` as the concurrent-redelivery backstop; duplicate `CallSid` → **200 no-op, zero second effect**. Missing `CallSid` → `4002`/400 (defensive). All effects run under a synthetic `TenantContext(tenantId, null, Set.of("INTEGRATION_TWILIO"))` via `contextWrite(TenantContextHolder.write(ctx))`.
- **Transcription seam (no net-new live STT this phase).** `VoicemailTranscriptionSource` + the default `InboundTranscriptionTextSource` (`@ConditionalOnMissingBean`) simply use Twilio's built-in `TranscriptionText`. A future OpenAI-Whisper STT impl on the fetched recording is the documented, default-OFF seam the plan §3 recommends — it would be a new bean implementing the interface, no change to the service. Keeps this phase free of any net-new live outbound STT (§7).
- **Extraction: `VoicemailExtractionService` mirrors `AnthropicAiAssistService` (new additive sibling — that core is empty-diff).** Same per-tenant key (`IntegrationConnection(anthropic).secrets.apiKey` → `kmosf.ai.anthropic.house-key` fallback → `1203`), same `AiUsageRecorder` budget gate (`checkBudget` before / `record` after), same configurable base-url `kmosf.ai.anthropic.base-url` (default real, **overridden → WireMock in every test**), same `x-api-key` POST shape (`1202` on non-2xx). Strict system prompt → ONLY JSON `{name, phone, address, problem, urgency, callbackRequested}`; **defensive parse** (strips code fences/prose; blank or parse-failure → `VoicemailExtraction.empty()`; never throws). Called **best-effort** (`onErrorResume(→empty)`) so a `1200` budget-exhausted / `1202` upstream error **never drops the lead** — AI is triage, not truth (plan §8); the raw transcript + recording are always attached.
- **Lead + Activity + notify (reused cores empty-diff).** Find-or-create a Contact by the caller `From` phone (new strictly-additive `ContactRepository.findByTenantAndPhoneNumber` `@Query` finder) via an **explicit-boolean** branch (`Optional` + `isPresent()` — never `switchIfEmpty(create)`; existing contact untouched, the ServiceRequest-widget precedent). Log an `Activity(type=CALL, direction=INBOUND, subjectType=CONTACT, subjectId=contact, summary=extracted one-liner, body=raw transcript, payload={callSid, recordingUrl, extractedJson, ...})` via the **UNCHANGED `ActivityCrudService.create`**. **Notify Rob** (best-effort, `onErrorResume`): `EmailService.sendSingleEmail` + `TwilioSmsService.sendSms` to per-tenant targets from `IntegrationConnection.config["notifyEmail"]`/`["notifyPhone"]` (**NOT hardcoded**; email `from` = `kmosf.mail.smtp.username`). **Auto-ack the caller**: `TwilioSmsService.sendSms` back to `From` (`kmosf.voicemail.auto-ack-message`). A send failure never fails the ingest (the lead + Activity are already durable). The ledger is back-filled with `resolvedContactId`/`createdActivityId`.
- **Advisory domain events** (`DomainEventType` Phase-1 block): `VOICEMAIL_RECEIVED` (after the ledger insert; payload `{callSid, fromNumber, transcriptionStatus}`), `VOICEMAIL_LEAD_CREATED` (after Contact + Activity; payload `{callSid, contactId, activityId}`). Neither drives a core mutation.
- **Module gate.** `@ConditionalOnProperty(prefix="kmosf.modules.voicemail-intake", name="enabled", matchIfMissing=true)` on the service AND both webhook controllers. **Deviation from the Phase-H "no gate on the webhook controller" convention** (necessary): `CalComWebhookService` has no gate at all, so gating only the `matchIfMissing=true` service while leaving the always-on controllers ungated would break Spring wiring (missing bean) when the module is explicitly disabled — gating both is the only internally-consistent way to honor the required module gate, and matches the `ServiceRequestWidgetController` precedent (disabled module → endpoint not registered → 404, "the correct outcome"). Recorded in `docs/PHASE-PROGRESS.md`.
- **Error range 4000-4099** (`GlobalErrorHandler` Javadoc — the next free block after H's 3900-3999): `4000` Twilio signature invalid (401); `4001` Twilio not connected for tenant (404 — Phase-1-local; `2510` remains the documented cross-integration fallback); `4002` params malformed / missing `CallSid` (400, defensive); `4003` invalid tenant id in path (400); reserved `4004-4099`. Reused (NOT re-allocated): `1200-1203` (AI budget gate / Anthropic upstream / missing-key — surfaced by `VoicemailExtractionService`), `2510`, `2530-2532` (reused `TwilioSmsService` send paths), `1300` (reused `ActivityCrudService`).
- **§7 HARD no-live-external boundary.** Twilio + Anthropic base URLs configurable; the Anthropic extraction call → WireMock via `@DynamicPropertySource(kmosf.ai.anthropic.base-url)` in every test; the notify/auto-ack SMS + email go through the existing `TwilioSmsService`/`EmailService` whose base URLs are not config-driven, so they are `@MockitoBean`'d in the IT (no live send; the `OnTheWayDispatchIT` precedent — the dispatch contract is still asserted). `authToken` + `apiKey` are sandbox fakes; no host hardcoded; no live call/charge/send/STT anywhere in the loop. **Provisioning a real Twilio number + conditional call-forwarding of Rob's line is a SEPARATE HUMAN ACTION** — never in this code/loop.
- **Reused cores empty-diff vs `main`** (verified `git diff main`, 0 lines each): `AnthropicAiAssistService`, `AiUsageRecorder`, `TwilioSmsService`, `EmailService`, `ContactCrudService`, `ActivityCrudService`, `IntegrationConnection`(+`Service`+`Repository`). Strictly-additive: the `ContactRepository` phone finder, the `TwilioVoicemailEvent` ledger + repo, the `DomainEventType` Phase-1 block, the config properties.
- **Tests.** `TwilioVoicemailIT` mirrors `CalComWebhookIT`/`StripeWebhookIdempotencyIT`/`DocumensoWebhookSignedIT` (signed callback → Contact + Activity + notify + auto-ack; invalid-sig → 401/4000 zero-effect; duplicate `CallSid` → 200 no-op exactly-once; not-connected → 4001; TwiML voice endpoint). `TwilioRequestValidatorTest` (self-computed known-good signature) + `TwilioVoicemailControllerUrlTest` (deterministic `reconstructFullUrl` proof) run without Docker. Shard-safe (self-clean `mongo.remove`; no `application-test.properties`/`build.gradle` shard change; the only mocks are the two precedented notify seams).
- **BE-only.** No FE this phase. The NMM-site photo section + admin review list + the photo-triage `MoleVisionService` (Feature B) and coverage-window automation are subsequent phases on this same shared intake spine (plan §5).

**Phase J — contractor / time-management vertical (IN PROGRESS — J1 SHIPPED this PR; J2–J4 follow).** Adds employee/contractor management on top of the shipped Project (Phase C) + Time-and-Expenses (Phase D) verticals so KMOSF can bring on 1099 teammates: a staff/contractor directory, project assignment with per-assignment bill/cost rates, contractor-scoped access, and submit→approve timesheets gating invoicing/payout. Driving plan: `~/.claude/plans/i-ve-stumbled-across-a-ancient-micali.md` (slug `contractor-time-mgmt`). New packages `model/contractor`, `repository/contractor`, `service/contractor`, `controller/contractor` (symmetric with Phase C/D). Error range **4100–4199** (`GlobalErrorHandler` Javadoc table; voicemail-intake holds 4000–4099). Module gate `kmosf.modules.contractor.enabled` (matchIfMissing=true).

Sub-phase **J1** (this PR — data model, directory, assignment, rate stamping):

- **`ProjectAssignment`** (`@Document("project_assignments")`, `Auditable`): the (project × user) join — `billRateOverride`/`costRateOverride`, free-text `role`, soft-delete `active`. Unique `(tenantId, projectId, userId)` makes assignment idempotent (201 first / 200 repeat, the `convertFromDeal` precedent; a soft-deleted row reactivates). A standalone doc (not an array on `Project`) because "list my assigned projects" is the highest-frequency contractor authz query.
- **`Timesheet`** (`@Document("timesheets")`, `Auditable`): per-(user, ISO-week) period, `status OPEN→SUBMITTED→APPROVED|REJECTED`. Created in J1; the submit/approve lifecycle + the `TimeEntry.approved` invoicing gate land in **J3**. Unique `(tenantId, userId, periodStart)`.
- **`User`** (additive nullable): `defaultBillRate`, `defaultCostRate`. New role token **`CONTRACTOR`** (a contractor is `roles={STAFF,CONTRACTOR}` — `STAFF` keeps them on the staff security chain, `CONTRACTOR` is the J2 scoping marker; zero auth-infra change — roles already flow through the JWT).
- **`TimeEntry`** (additive nullable): `costRateAmount` (cost rate, symmetric with the existing bill `rateAmount`), `timesheetId` (FK to the period), `approved` (the J3 invoicing gate, default false) + index `tenant_timesheet_idx`.
- **Rate + period stamping** (`TimeEntryService.create`/`startTimer`/`stopTimer`/`update`): rates resolve once on the body (explicit body → `ProjectAssignment` override → `User` default → null; a null bill still uses the existing invoice-time `defaultRateAmount` fallback, a null cost is surfaced by the J4 payout report); the period is found-or-created **per split segment** (reuses `TimeSplitService.resolveZone` so the Mon–Sun week boundary aligns with the midnight-split boundary — a week-straddling session lands its halves in two periods). Explicit-boolean find-or-create (never `switchIfEmpty(create)`); unique-index `DuplicateKey` backstop.
- **`TeamController`** (`/team`, ADMIN): staff/contractor directory CRUD + invite — the first user-management surface (users were otherwise created only by `TenantBootstrapService` + portal invite). Returns `TeamMemberView` projections (no `passwordHash` leak). `CONTRACTOR` implies `STAFF`; password ⇒ ACTIVE else INVITED.
- **`ProjectAssignmentController`** (`/projects/{projectId}/assignments`, ADMIN, `@IdempotentRoute` on assign): list / assign (idempotent) / update-rates / soft-delete.
- **Advisory events** (`DomainEventType` Phase-J block): `PROJECT_ASSIGNED`, `PROJECT_UNASSIGNED` — non-driving.
- **§9 honored:** every conditional-create is explicit-boolean; `switchIfEmpty` only for genuine not-found (4101/4106/4140).

Sub-phases **J2** (contractor scoped access — `/me/contractor/**` + `RoleGuard.denyRole`, codes 4130–4135), **J3** (timesheet submit/approve + approved-only invoicing gate, codes 4120/4150/4151), **J4** (payout + margin report) follow as separate PRs per the driving plan.

## On-demand reference files

When running, building, or testing locally, read `.claude/commands.md`.
When navigating packages or understanding subsystem responsibilities, read `.claude/architecture.md`.
When writing or running tests, read `.claude/testing.md`.
When choosing what to build next or checking if a feature exists, read `.claude/roadmap.md`.

## Stack

- **Spring Boot 3.5.6** on **Java 21** (toolchain pinned in `build.gradle`)
- **Reactive end-to-end**: `spring-boot-starter-webflux` (Netty), `spring-boot-starter-data-mongodb-reactive` with `ReactiveMongoRepository`, reactive Resilience4j. All controllers return `Mono`/`Flux`; any blocking I/O (JDBC, `jakarta.mail.Transport.send`, PDF generation) is wrapped in `Mono.fromCallable(...).subscribeOn(Schedulers.boundedElastic())`. Do not introduce blocking patterns on Netty event-loop threads.
- **Jakarta Mail** through a hand-rolled `Session` bean (`AngusConfig`) talking to ProtonMail SMTP — *not* `JavaMailSender`
- **Spring's `@Scheduled`** is in active use (ReportScheduler, SlaBreachScheduler, SequenceEngine, etc.). **Quartz** is on the classpath but not yet wired — stop-gap only; see roadmap.
- **MapStruct 1.6.3** + **Lombok** as annotation processors. Order in `build.gradle` matters — don't reorder the `annotationProcessor` block without verifying MapStruct still generates implementations.
- **Spring Cloud Circuit Breaker (Resilience4j, reactor)** is in active use: `WebhookDeliveryService` and `QuickBooksInvoiceSync` both use per-subscription circuit breakers.
- **Nimbus JOSE + JWT** for HS256 JWT signing/verification (self-issued). `kmosf.auth.mode=local|zitadel` switches the `ReactiveJwtDecoder` (Phase A). **Zitadel mode is now end-to-end (Phase A2):** the single dual-mode `JwtTenantResolver` resolves tenant from the `urn:zitadel:iam:org:id` claim via `Tenant.zitadelOrgId` (sparse-unique; also the per-tenant portal Zitadel opt-in flag) + a cached `ZitadelOrgTenantCache`, maps the `urn:zitadel:iam:org:project:roles` claim (a JSON object) through `kmosf.auth.zitadel.role-map`, JIT-provisions the `User` (idempotent via the existing `tenant_email_idx`), and `POST /auth/login` returns 410 in zitadel mode. The `local` branch is byte-identical to pre-A2. Zitadel-runtime errors use range **3300-3399** (3300 missing-org/401, 3301 unknown-org/403, 3302 no-role/403, 3303 login-gone/410); 3200-3299 stays auth-mode startup/config. Server-side portal Zitadel `oauth2Login` **shipped in Phase H** (not A2 — opt-in per `Tenant.zitadelOrgId`; A2 left `PortalSecurityConfig` untouched, Phase H extended it **additively** so the non-opted local magic-link/passkey path stays byte-identical; the FE may still drive the OIDC redirect via `/auth/discovery`). See the Phase-H SHIPPED note below.

`build.gradle` carries commented-out starters (Kafka, Spring Integration MongoDB, session-data-mongodb, OTLP registry). These are aspirational scaffolding — don't delete them when cleaning up.

## Architecture overview

Layered Spring WebFlux structure under `com.kumouri.kmodigipresbe`. All tenant-owned repositories extend `TenantScopedSimpleReactiveMongoRepository` (not bare `ReactiveMongoRepository`) — this auto-stamps and auto-filters by `tenantId`. See `.claude/architecture.md` for the full package overview and conventions.

## Safety rules (apply every session)

- **SMTP credentials are externalized** via `@Value` in `AngusConfig`. Never hardcode credentials. The app fails to start if `KMOSF_MAIL_SMTP_PASSWORD` is absent — this is intentional.
- **JWT secret** (`KMOSF_JWT_SECRET`) must be >= 32 bytes for production. If unset, a random ephemeral key is generated and a WARN is logged — tokens invalidate on every restart; not suitable for production.
- **Blocking I/O must use `Schedulers.boundedElastic()`** — never call blocking code on a Netty event-loop thread.

## Branching

This repo follows the workspace branching conventions (see `../../CLAUDE.md`): `<plan-slug>` for single-phase plans, `<plan-slug>-phase-N-<desc>` only when one plan is split, `fix/<desc>` and `feat/<desc>` for unplanned work. Default branch is `main`; don't work directly on it.
