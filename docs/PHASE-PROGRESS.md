# NMM GBP review-reply automation — Progress Ledger

> Crash-recovery source of truth. Each sub-phase is its own commit; this row is set
> →done+results (committed) as the last action of every sub-phase. A DRAFT PR is opened
> after 4G.1 compiles and `git push` runs after EVERY sub-phase commit (the crash-hedge).
> Plan/context: `i-had-an-idea-ticklish-rivest.md` — a candidate-workflow pivot (replaces the
> original thin "neighbor lead-gen" Phase 4; "GBP review-reply automation" was on NMM's
> candidate-workflow list), NOT a numbered §5 Phase. Builds on the merged Phase-1 (voicemail)
> + Phase-2 (photo-triage) + Phase-3 (coverage-window) NMM AI-intake spine.

## Branch: nmm-gbp-review-reply-automation  (base main @ 6f3e9cb — the merge that includes Phases 1+2+3)
## Model: Opus implementer = Opus 4.8 (1M context). BE-only — no FE. No live external services (§7 — WireMock/sandbox/.invalid throughout; the poller default-OFF; live Google OAuth/GBP is a deferred, Google-approval-gated human step).

| Sub-phase | Status | SHA | Build (compileJava+compileTestJava) | Mandated checks | Deviations |
|---|---|---|---|---|---|
| 4G.1 error range 4030-4049 (claims Phase-2's reserved 4030-4039, extended to 4049) + DomainEventType GBP block + GbpReviewReply ledger/draft entity + repo + GbpProperties/GbpConfig + config props | done | (this commit) | compileJava BUILD SUCCESSFUL exit 0 | GlobalErrorHandler Javadoc: 4030 GBP not connected (404, Phase-local; 2510 cross-integration fallback), 4031 GBP API fetch/post failed (502), 4032 review-reply draft not found (404), 4033 review-reply not DRAFTED — cannot post/skip (409, defensive); reserved 4034-4049; reused not re-allocated 1200-1203 (AI — GbpReplyDraftService), 2530-2532 (Twilio SMS — reused TwilioSmsService notify), 1300 (Activity), 1800 (RoleGuard ADMIN). DomainEventType +2 advisory constants (GBP_REVIEW_REPLY_DRAFTED / GBP_REVIEW_REPLY_POSTED). GbpReviewReply (@Document gbp_review_replies, TenantScoped NOT Auditable — the CalComWebhookEvent/RecurringInvoiceOccurrence rationale) = idempotency ledger (unique tenant_review_idx {tenantId,reviewId}, ledger-insert-FIRST) AND the persisted draft Rob approves: {reviewId, rating, comment, reviewerName, reviewCreateTime, draftedReply, status DRAFTED→POSTED|SKIPPED, postedAt}. GbpReviewReplyRepository: findByTenantIdAndReviewId (the explicit-boolean probe — NEVER switchIfEmpty(process)), findByTenantIdAndStatusOrderByReceivedAtDesc + findByTenantIdAndId (explicit tenantId predicates). GbpProperties (kmosf.gbp.api-base-url default https://gbp.googleapis.invalid — non-routable .invalid; requestTimeoutSeconds) registered via GbpConfig @EnableConfigurationProperties (the DocumensoConfig precedent). Config props: kmosf.modules.gbp-reviews.{enabled=false (DEFAULT-OFF poller), auto-post=false, poll-interval-ms, initial-delay-ms}, kmosf.gbp.{api-base-url, request-timeout-seconds, draft-model=sonnet, reply-system-prompt}, resilience4j gbp-reviews breaker (the stripe-checkout pattern). | none |
| 4G.2 GbpApiClient (raw WebClient, configurable base-url → WireMock, Resilience4j gbp-reviews breaker: fetchReviews + postReply) + GbpReplyDraftService (mirrors VoicemailExtractionService/AnthropicAiAssistService — NEW additive sibling, the core stays empty-diff) | done | (this commit) | compileJava BUILD SUCCESSFUL exit 0 | GbpApiClient (raw WebClient, NO Google SDK — the DocumensoClient/StripeCheckoutService precedent): fetchReviews() + postReply(reviewId, replyText). TenantContextHolder.required() → resolveConnection(provider="google-business") [switchIfEmpty → 4030 not-connected, GENUINE not-found] → accessToken from secrets["accessToken"] (4030 if missing) → base-url precedence config["apiBaseUrl"] → GbpProperties.apiBaseUrl (default https://gbp.googleapis.invalid non-routable .invalid; §7) → Mono.defer + .cache() + transformDeferred(CircuitBreakerOperator gbp-reviews) + Retry.backoff(3,1s).maxBackoff(4s) filtering CallNotPermitted/Unauthorized/DigiPresBe + .timeout(requestTimeoutSeconds) → onErrorMap → 4031 (502). Adapter boundary: the assumed GBP wire shape (GET /v4/{locationName}/reviews → {reviews:[{reviewId|name,starRating ONE..FIVE→1..5,comment,reviewer.displayName,createTime}]}; PUT /v4/{reviewId}/reply {"comment":...}) lives ONLY in fetch/post + the 2 parse helpers (locationName from config, defaults to a /v4/reviews relative path the WireMock stub matches). GbpReview record {reviewId,rating,comment,reviewerName,createTime}. GbpReplyDraftService = NEW additive sibling mirroring VoicemailExtractionService/AnthropicAiAssistService EXACTLY: per-tenant apiKey (anthropic secrets.apiKey → kmosf.ai.anthropic.house-key fallback → 1203; the ONLY switchIfEmpty = this genuine key fallback) + AiUsageRecorder.checkBudget before / record after + configurable kmosf.ai.anthropic.base-url (→ WireMock in tests) + x-api-key POST (1202 non-2xx) + kmosf.gbp.draft-model (default sonnet). Warm concise professional small-local-business reply system prompt (thanks reviewer, addresses specifics, low-rating → gracious + make-it-right), configurable via kmosf.gbp.reply-system-prompt; output = ONLY reply text; blank model answer → 1202 (never persist a blank reply). Propagates 1200/1202/1203 (the poller wraps best-effort). AnthropicAiAssistService/AiUsageRecorder NOT modified (empty-diff). | none |
| 4G.3 GbpReviewPoller (@Scheduled + @ConditionalOnProperty gbp-reviews matchIfMissing=false DEFAULT-OFF; per connected tenant fetch → explicit-boolean review-id idempotency + ledger-insert-FIRST → draft best-effort → persist DRAFTED → notify Rob → GBP_REVIEW_REPLY_DRAFTED; optional auto-post → postReply + POSTED + GBP_REVIEW_REPLY_POSTED) | done | (this commit) | compileJava BUILD SUCCESSFUL exit 0 | GbpReviewPoller: @Component @ConditionalOnProperty(prefix=kmosf.modules.gbp-reviews, name=enabled, matchIfMissing=FALSE) — DEFAULT-OFF (the ImapInboundPoller/CoverageNudgeJob precedent: the bean is not created in CI/any default run → ZERO live Google fetch → §7 satisfied by construction; the GbpApiClient base-url also defaults to a non-routable .invalid). @Scheduled(fixedRate default 15min, initialDelay 60s) tick fire-and-forget subscribes on the scheduler thread (NEVER Netty); visible-for-test pollOnce() returns Mono<Void> the IT blocks (the CoverageNudgeJob.nudgeDueOnce() pattern). Cross-tenant sweep = tenants.findAll().concatMap(pollTenant) — pollTenant ONLY proceeds if connections.findByTenantIdAndProvider("google-business") is present (a non-GBP tenant silently skipped, never a 4030 error), fetches under synthetic TenantContext(tenantId,null,AUTOMATION_GBP_REVIEWS) via contextWrite. Per review: EXPLICIT-BOOLEAN review-id probe (findByTenantIdAndReviewId.map(true).defaultIfEmpty(false)) → if unseen: ledger-insert-FIRST (GbpReviewReply DRAFTED + the review snapshot, draftedReply=null, save with unique tenant_review_idx, onErrorResume(DuplicateKeyException→empty) concurrent-repoll backstop) → draft best-effort via GbpReplyDraftService (onErrorResume→empty + defaultIfEmpty(saved): an AI 1200/1202 leaves the review LEDGERED with null draft, Rob still sees it) → persist draft onto the row → notify Rob (email+SMS to per-tenant Twilio config notifyEmail/notifyPhone, NOT hardcoded; onErrorResume best-effort) → GBP_REVIEW_REPLY_DRAFTED. Optional auto-post (kmosf.modules.gbp-reviews.auto-post, default false): if true AND a draft exists → GbpApiClient.postReply → status POSTED + postedAt + GBP_REVIEW_REPLY_POSTED; a post failure is swallowed (draft stays DRAFTED — never lost). Per-tenant + per-review onErrorResume so one failure never aborts the sweep. §9: NEVER switchIfEmpty(process/create); the only switchIfEmpty-style construct is the explicit not-seen boolean default; WebClient non-blocking + reactive draft, no .block() on the loop. | none — a clean cross-tenant sweep via tenants.findAll() + the per-tenant google-business connection pre-filter (the CoverageNudgeJob precedent); no new provider-only repo finder needed |
| 4G.4 GbpReviewReplyAdminController (ADMIN-guarded, @ConditionalOnProperty gbp-reviews matchIfMissing=true: GET list DRAFTED + POST {id}/post + POST {id}/skip) | done | (this commit) | compileJava BUILD SUCCESSFUL exit 0 | GbpReviewReplyAdminController @RequestMapping("/gbp/review-replies") (base-path /api/v1 applied by spring.webflux.base-path) @ConditionalOnProperty(prefix=kmosf.modules.gbp-reviews, name=enabled, matchIfMissing=TRUE) — the ALWAYS-registerable admin surface, DISTINCT from the matchIfMissing=FALSE default-OFF poller (so a deployment can leave the poller off while the admin surface stays available; turning the whole module off removes both). GET (RoleGuard ADMIN, 1800) → list DRAFTED most-recent-first; POST /{id}/post (RoleGuard ADMIN + @IdempotentRoute — the strongest candidate in the phase since it posts to Google; the spawn-now precedent; returns the updated entity for the response tee) optional @RequestBody PostReplyRequest{reply} (null/blank → keep AI draft); POST /{id}/skip (RoleGuard ADMIN, no @IdempotentRoute — pure local status flip, no external effect, the setStatus precedent). GbpReviewReplyAdminService: listDrafted / post(id, editedReply) / skip(id) — tenant from the active authenticated context (the RecurringInvoiceService precedent); findByTenantIdAndId.switchIfEmpty → 4032 (GENUINE not-found); explicit status==DRAFTED guard → 4033 (NEVER switchIfEmpty(process)); post → reuse GbpApiClient.postReply (the SAME client the poller uses — no second posting path) → POSTED + postedAt + GBP_REVIEW_REPLY_POSTED; skip → SKIPPED (no Google call). §9: switchIfEmpty only for genuine not-found (4032); the not-DRAFTED rejection is an explicit boolean status check (4033). | none — @IdempotentRoute on /post (external Google effect, the spawn-now precedent), not on /skip (local-only); the DRAFTED→POSTED status machine is itself the double-post guard (a 2nd post → 4033), @IdempotentRoute is the belt |
| 4G.5 BE ITs (GbpReviewPollerIT poller-opted-ON; GbpReviewReplyAdminIT; GbpReplyDraftServiceIT) GREEN + the module-gate decouple fix | done | (this commit) | compileTestJava BUILD SUCCESSFUL exit 0; forced cleanTest run GREEN (Docker up): GbpReplyDraftServiceIT 4/0/0/0, GbpReviewPollerIT 2/0/0/0, GbpReviewReplyAdminIT 6/0/0/0, OpenApiEndpointIT 2/0/0/0 | GbpReviewPollerIT (2, poller opted-ON in-test): WireMock GBP GET /v4/reviews returns 2 reviews (FIVE→5, TWO→2) → 2 GbpReviewReply(DRAFTED) persisted each with a WireMock-Anthropic draft + the review snapshot (rating/reviewer/comment) + notify(email→notifyEmail + 2 SMS→notifyPhone, the @MockitoBean seams) + GBP fetch(1)+drafts(2) hit WireMock + NO PUT/reply (auto-post OFF) + 2 GBP_REVIEW_REPLY_DRAFTED events (no POSTED); re-poll same ids → idempotent: still exactly 2 rows, fetch ran twice but drafts/notify/events fired only twice TOTAL (the explicit-boolean review-id probe skipped both before drafting). GbpReviewReplyAdminIT (6, ADMIN JWT the ProjectAssignmentCrudIT pattern): GET lists only DRAFTED (a POSTED row excluded); GET non-admin → 403/1800; POST /{id}/post with an edited reply → the edited text PUT to WireMock GBP (matchingJsonPath $.comment) → row POSTED + postedAt; POST /{id}/post no google-business connection → 404/4030, row stays DRAFTED, zero WireMock; POST /{id}/skip → SKIPPED, zero WireMock; POST non-existent id → 404/4032. GbpReplyDraftServiceIT (4): 5★ review → drafted reply + the prompt carried "5 out of 5" + the comment (matchingJsonPath, §7 base-url); 1★ critical review → still drafts the gracious "make it right" path + prompt carried "1 out of 5"; upstream 500 → 1202 no crash; blank model answer → 1202 (never a blank draft). §7: GBP + Anthropic base-urls → ONE WireMock via @DynamicPropertySource (disjoint paths /v4/* vs /); TwilioSmsService + EmailService @MockitoBean; accessToken + apiKey sandbox fakes; poller default-OFF (opted-ON only in GbpReviewPollerIT). Shard-safe: only the 2 precedented notify mocks + WireMock @DynamicPropertySource; self-clean mongo.remove; no application-test.properties/build.gradle shard change. | MODULE-GATE DECOUPLE FIX (recorded): the first admin-IT run hit 404 on every endpoint because application.properties carried `kmosf.modules.gbp-reviews.enabled=false` — that makes the property PRESENT, which overrode the admin controller's matchIfMissing=TRUE to false (a disabled controller → 404). FIX: removed the `enabled` line entirely (the coverage-nudge/mole-tripwire/mole-triage precedent — they carry NO enabled line); now ABSENCE = poller matchIfMissing=false stays OFF + admin controller matchIfMissing=true stays ON (the two intentionally decoupled by differing matchIfMissing). Opt the poller in via KMOSF_MODULES_GBP_REVIEWS_ENABLED=true; setting the property false explicitly turns both off. No code change — properties only. |
| 4G.6 docs (CLAUDE.md GBP section + openapi.json regen) + final ITs GREEN + mark PR ready | pending | | | | |

## No-live-external boundary (§7) — recorded
- [x] GBP + Anthropic base URLs configurable and pointed at WireMock in every test; poller default-OFF; notify seams @MockitoBean; OAuth creds/keys sandbox fakes; NO host hardcoded; NO live OAuth/post/charge/send anywhere. — The GBP API call is `kmosf.gbp.api-base-url` (default `https://gbp.googleapis.invalid`, a non-routable .invalid; per-tenant `IntegrationConnection.config["apiBaseUrl"]` takes precedence; → WireMock via `@DynamicPropertySource` in GbpReviewPollerIT + GbpReviewReplyAdminIT). The Anthropic reply-draft call is the SHARED `kmosf.ai.anthropic.base-url` (→ WireMock; the reused-pattern property — empty-diff to AnthropicAiAssistService). The notify SMS + email go through the EXISTING TwilioSmsService/EmailService (base URLs not config-driven) → `@MockitoBean`'d in GbpReviewPollerIT (no live send; the MoleTriageIT/OnTheWayDispatchIT precedent — dispatch contract asserted via captured to-targets). The GbpReviewPoller is DEFAULT-OFF (`@ConditionalOnProperty(...gbp-reviews.enabled, matchIfMissing=FALSE)` + NO enabled line in application.properties → absence = OFF) so the bean is not created in CI / any default run → ZERO live Google fetch; GbpReviewPollerIT opts it in explicitly. accessToken (`ya29.…-fake`) + apiKey (`sk-ant-test-…`) are sandbox fakes. NO host hardcoded; NO live OAuth/post/charge/send anywhere. Wiring live Google OAuth / GBP API access (the My Business / Business Information API) is a SEPARATE, Google-approval-gated HUMAN ACTION — never in this code/loop.

## §9 reactive invariant — recorded
- [x] switchIfEmpty over the new integration/gbp package + admin controller = genuine not-found only; ZERO switchIfEmpty(create/process/draft/post). — grep over integration/gbp/ + GbpReviewReplyAdminController: exactly FOUR operational hits, all genuine not-found/no-key → GbpApiClient:140 (google-business not-connected → 4030), GbpReplyDraftService:111 (the reused house-key fallback, verbatim AnthropicAiAssistService — "no key" → 1203), GbpReviewReplyAdminService:66 + :102 (the draft load → 4032); the rest are Javadoc. The review-id idempotency seam is EXPLICIT-BOOLEAN (findByTenantIdAndReviewId.map(true).defaultIfEmpty(false)) + ledger-insert-FIRST (GbpReviewReply unique tenant_review_idx, onErrorResume(DuplicateKeyException→empty) concurrent backstop), NEVER switchIfEmpty(process); the admin not-DRAFTED rejection is an explicit boolean status check (4033). Blocking work off the Netty loop: GbpApiClient's WebClient is non-blocking; the JSON/draft work is reactive; no .block() on the loop; the @Scheduled tick subscribes on the scheduler pool.
- [x] Reused cores empty-diff vs the base 6f3e9cb (NOT local `main`): AnthropicAiAssistService / AiUsageRecorder / TwilioSmsService / EmailService / ActivityCrudService / IntegrationConnection(+Repository) — `git diff 6f3e9cb --stat` over all of them = ZERO lines. Strictly-additive: the new integration/gbp/* package (GbpApiClient/GbpReview/GbpReplyDraftService/GbpReviewPoller/GbpReviewReplyAdminService/GbpProperties/GbpConfig), GbpReviewReplyAdminController, the GbpReviewReply ledger + GbpReviewReplyRepository, the DomainEventType GBP block (+2 constants), the GlobalErrorHandler 4030-4049 Javadoc, the config props, and the openapi.json regen.

## Baseline note (IMPORTANT for the validator)
The branch is based on `6f3e9cb` (the merge that includes Phases 1+2+3). Reused-cores empty-diff
MUST be verified against `6f3e9cb` (the correct base), NOT local `main` (which may be behind that
merge — a `git diff main` would falsely show Phase-1/2/3 files as new, the Phase-2/3 lesson).

## Opus implementer note
Implemented by Opus 4.8 (1M context) as a single supervised pass; Docker is up so the ITs are run
to GREEN locally before the PR is marked ready (the non-negotiable gate). Crash-hedge: push + open
a DRAFT PR as soon as 4G.1 compiles, `git push` after EVERY sub-phase commit, mark the PR ready
only at the end. Each sub-phase: explicit `git add <paths>` (never -A/.; never the root CLAUDE.md
or `.claude/*`), compileJava+compileTestJava after each, full ITs to GREEN at the end.
