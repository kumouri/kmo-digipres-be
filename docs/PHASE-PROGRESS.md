# Phase 1 (NMM AI intake) — Voicemail-to-Lead Pipeline — Progress Ledger

> Crash-recovery source of truth. Each sub-phase is its own commit; this row is set
> →in-progress (committed) as the first action and →done+results (committed) as the
> last action of every sub-phase. Plan: `i-had-an-idea-ticklish-rivest.md` §2/§3/§5/§6
> (the NMM AI intake layer; Feature A — voicemail-to-lead). The pipeline is a faithful
> structural mirror of the shipped Cal.com webhook (Phase H — `CalComWebhookService`).

## Branch: nmm-ai-intake-phase-1-voicemail-to-lead  (base main @ 2b24df8)
## Model: Implement=Opus 4.8 (1M context). BE-only — no FE this phase. No live external services (§7).

| Sub-phase | Status | SHA | Build (compileJava+compileTestJava) | Mandated checks | Deviations |
|---|---|---|---|---|---|
| 1.1 error range 4000-4099 + DomainEventType Phase-1 block + ledger entity/repo + additive Contact phone finder + config props | done | f172ecb | BUILD SUCCESSFUL true-exit 0 (9s) | error-code block 4000-4099 added to GlobalErrorHandler Javadoc only (no handler code touched, no existing `<li>` modified); DomainEventType +2 advisory constants (VOICEMAIL_RECEIVED/VOICEMAIL_LEAD_CREATED) before private ctor; TwilioVoicemailEvent = TenantScoped NOT Auditable (CalComWebhookEvent mirror), unique compound tenant_callsid_idx {tenantId,callSid}; TwilioVoicemailEventRepository.findByTenantIdAndCallSid (explicit-boolean probe finder, explicit tenantId predicate); ContactRepository.findByTenantAndPhoneNumber = strictly-additive @Query finder (existing email finder + ContactCrudService unchanged); config props additive (module gate matchIfMissing=true, greeting/auto-ack/extraction-model, anthropic base-url surfaced) | none |
| 1.2 TwilioRequestValidator + voice/voicemail controllers + TwilioVoicemailService skeleton (verify→idempotency→ledger-first→synthetic ctx) | done | a2474bf | BUILD SUCCESSFUL true-exit 0 (5s) | TwilioRequestValidator = HMAC-SHA1(authToken, fullUrl + concat(sorted POSTkey+value)) base64, constant-time compare, null-safe, no-throw — the ONLY place the Twilio signing scheme lives (CalComSignatureVerifier mirror); buildSignedString package-private for unit test. Controllers: TwilioVoiceController (POST /public/integrations/twilio/{tenantId}/voice, produces application/xml) + TwilioVoicemailController (POST .../voicemail) — tenant from @PathVariable ONLY (4003 on bad UUID), reconstructFullUrl honors X-Forwarded-Proto/Host then request URI (proxy-aware), shared static. TwilioVoicemailService: verifiedConnection (4001 not-connected via genuine switchIfEmpty, 4001 missing authToken, 4000 sig-invalid via TwilioRequestValidator) precedes process; explicit-boolean CallSid idempotency probe (findByTenantIdAndCallSid.map(true).defaultIfEmpty(false).flatMap(seen -> seen ? Mono.empty() : processAndRecord)) — NOT switchIfEmpty(process); ledger-insert-FIRST (voicemailEvents.save before any effect) + onErrorResume(DuplicateKeyException→Mono.empty()); synthetic TenantContext(tenantId,null,Set.of("INTEGRATION_TWILIO")) via contextWrite; 4002 on missing CallSid; emits advisory VOICEMAIL_RECEIVED; transcription seam (VoicemailTranscriptionSource + default InboundTranscriptionTextSource using inbound TranscriptionText, @ConditionalOnMissingBean for a future Whisper impl). VoicemailCallbackParams/VoicemailTranscription/VoicemailExtraction value types. handleVoice returns TwiML (Say greeting + Record transcribe=true transcribeCallback=.../voicemail maxLength playBeep). | DEVIATION: @ConditionalOnProperty(kmosf.modules.voicemail-intake, matchIfMissing=true) placed on BOTH webhook controllers AND the service (not service-only). The briefing said "no @ConditionalOnProperty on the webhook controller" but CalComWebhookService has NO gate at all; gating only the matchIfMissing=true service while leaving the always-on controllers ungated would break Spring wiring on explicit enabled=false (missing-bean). Gating both (the ServiceRequestWidgetController precedent: disabled module → endpoint not registered → 404 "the correct outcome") is the only internally-consistent way to honor the explicitly-required module gate. Reversible annotation-only choice; flag-or-fix step-1 reversible → best-pattern + documented. |
| 1.3 VoicemailExtractionService (Anthropic, mirrors AnthropicAiAssistService) + VoicemailTranscription seam | done | (this commit) | BUILD SUCCESSFUL true-exit 0 (4s) | VoicemailExtractionService = new additive sibling of AnthropicAiAssistService (NOT a modification): same resolveKey (per-tenant IntegrationConnection(anthropic).secrets.apiKey → kmosf.ai.anthropic.house-key fallback → 1203/412), same AiUsageRecorder.checkBudget() BEFORE + record() AFTER, same configurable base-url kmosf.ai.anthropic.base-url (default real, → WireMock in tests), same x-api-key/anthropic-version POST + 1202/502 on non-2xx. Strict system prompt → ONLY JSON {name,phone,address,problem,urgency,callbackRequested}; defensive parse (extractJsonObject strips fences/prose; blank/parse-fail → VoicemailExtraction.empty(); never throws). Blank transcript short-circuits to empty without spend. Wired into TwilioVoicemailService.handleTranscript best-effort: extract(...).onErrorResume(→empty) so a 1200 budget / 1202 upstream NEVER drops the lead (AI is triage not truth, plan §8). EMPTY-DIFF vs main verified: AnthropicAiAssistService + AiUsageRecorder both 0-line diff (git diff main). | Error codes reused not re-allocated (1200/1201 budget gate, 1202 upstream, 1203 missing-key — all the AI range surfaced by the same call shape as the exemplar). |
| 1.4 lead (Contact find-or-create) + Activity(CALL,INBOUND) + notify Rob + auto-ack caller (best-effort) | pending | — | — | — | — |
| 1.5 BE ITs (signed callback / invalid-sig / duplicate CallSid / not-connected / TwiML voice) + TwilioRequestValidator unit test | pending | — | — | — | — |
| 1.6 docs in-PR (CLAUDE.md Phase-1 section + openapi.json) + final BE green + PR | pending | — | — | — | — |

## No-live-external boundary (§7) — to verify
- [ ] Twilio + Anthropic base URLs configurable and pointed at WireMock in every test; auth token / api key are sandbox fakes; NO host hardcoded; NO live call/charge/send anywhere. Provisioning a real Twilio number + call-forwarding is a SEPARATE HUMAN ACTION — never in this code/loop.

## External-ingress trust (the §9 #1 invariant) — to verify
- [ ] verify-before-effect (X-Twilio-Signature via TwilioRequestValidator) precedes any effect; tenant from URL path NEVER payload; ledger-insert-FIRST (TwilioVoicemailEvent.save) under unique tenant_callsid_idx + DuplicateKeyException→Mono.empty(); synthetic TenantContext(...,INTEGRATION_TWILIO); duplicate redelivery → 200 no-op zero second effect.
- [ ] switchIfEmpty over the new package = genuine not-found / 4001 only; ZERO switchIfEmpty(process/create) on the idempotency seam.
- [ ] Reused cores empty/additive diff: AnthropicAiAssistService / TwilioSmsService / EmailService / ContactCrudService / ActivityCrudService / IntegrationConnection*.
