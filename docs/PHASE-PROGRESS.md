# Phase 1 (NMM AI intake) — Voicemail-to-Lead Pipeline — Progress Ledger

> Crash-recovery source of truth. Each sub-phase is its own commit; this row is set
> →in-progress (committed) as the first action and →done+results (committed) as the
> last action of every sub-phase. Plan: `i-had-an-idea-ticklish-rivest.md` §2/§3/§5/§6
> (the NMM AI intake layer; Feature A — voicemail-to-lead). The pipeline is a faithful
> structural mirror of the shipped Cal.com webhook (Phase H — `CalComWebhookService`).

## Branch: nmm-ai-intake-phase-1-voicemail-to-lead  (base main @ 2b24df8)
## Model: Implement=Opus 4.8 (1M context). BE-only — no FE this phase. No live external services (§7).

| Sub-phase | Status | SHA | Build (compileJava+compileTestJava) | Mandated checks | Deviations |
|---|---|---|---|---|---|
| 1.1 error range 4000-4099 + DomainEventType Phase-1 block + ledger entity/repo + additive Contact phone finder + config props | done | f172ecb | BUILD SUCCESSFUL true-exit 0 (9s) | error-code block 4000-4099 added to GlobalErrorHandler Javadoc only (no handler code touched, no existing `<li>` modified); DomainEventType +2 advisory constants (VOICEMAIL_RECEIVED/VOICEMAIL_LEAD_CREATED) before private ctor; TwilioVoicemailEvent = TenantScoped NOT Auditable (CalComWebhookEvent mirror), unique compound tenant_callsid_idx {tenantId,callSid}; TwilioVoicemailEventRepository.findByTenantIdAndCallSid (explicit-boolean probe finder, explicit tenantId predicate); ContactRepository.findByTenantAndPhoneNumber = strictly-additive @Query finder (existing email finder + ContactCrudService unchanged); config props additive (module gate matchIfMissing=true, greeting/auto-ack/extraction-model, anthropic base-url surfaced) | none |
| 1.2 TwilioRequestValidator + voice/voicemail controllers + TwilioVoicemailService skeleton (verify→idempotency→ledger-first→synthetic ctx) | done | a2474bf | BUILD SUCCESSFUL true-exit 0 (5s) | TwilioRequestValidator = HMAC-SHA1(authToken, fullUrl + concat(sorted POSTkey+value)) base64, constant-time compare, null-safe, no-throw — the ONLY place the Twilio signing scheme lives (CalComSignatureVerifier mirror); buildSignedString package-private for unit test. Controllers: TwilioVoiceController (POST /public/integrations/twilio/{tenantId}/voice, produces application/xml) + TwilioVoicemailController (POST .../voicemail) — tenant from @PathVariable ONLY (4003 on bad UUID), reconstructFullUrl honors X-Forwarded-Proto/Host then request URI (proxy-aware), shared static. TwilioVoicemailService: verifiedConnection (4001 not-connected via genuine switchIfEmpty, 4001 missing authToken, 4000 sig-invalid via TwilioRequestValidator) precedes process; explicit-boolean CallSid idempotency probe (findByTenantIdAndCallSid.map(true).defaultIfEmpty(false).flatMap(seen -> seen ? Mono.empty() : processAndRecord)) — NOT switchIfEmpty(process); ledger-insert-FIRST (voicemailEvents.save before any effect) + onErrorResume(DuplicateKeyException→Mono.empty()); synthetic TenantContext(tenantId,null,Set.of("INTEGRATION_TWILIO")) via contextWrite; 4002 on missing CallSid; emits advisory VOICEMAIL_RECEIVED; transcription seam (VoicemailTranscriptionSource + default InboundTranscriptionTextSource using inbound TranscriptionText, @ConditionalOnMissingBean for a future Whisper impl). VoicemailCallbackParams/VoicemailTranscription/VoicemailExtraction value types. handleVoice returns TwiML (Say greeting + Record transcribe=true transcribeCallback=.../voicemail maxLength playBeep). | DEVIATION: @ConditionalOnProperty(kmosf.modules.voicemail-intake, matchIfMissing=true) placed on BOTH webhook controllers AND the service (not service-only). The briefing said "no @ConditionalOnProperty on the webhook controller" but CalComWebhookService has NO gate at all; gating only the matchIfMissing=true service while leaving the always-on controllers ungated would break Spring wiring on explicit enabled=false (missing-bean). Gating both (the ServiceRequestWidgetController precedent: disabled module → endpoint not registered → 404 "the correct outcome") is the only internally-consistent way to honor the explicitly-required module gate. Reversible annotation-only choice; flag-or-fix step-1 reversible → best-pattern + documented. |
| 1.3 VoicemailExtractionService (Anthropic, mirrors AnthropicAiAssistService) + VoicemailTranscription seam | done | 78c7acc | BUILD SUCCESSFUL true-exit 0 (4s) | VoicemailExtractionService = new additive sibling of AnthropicAiAssistService (NOT a modification): same resolveKey (per-tenant IntegrationConnection(anthropic).secrets.apiKey → kmosf.ai.anthropic.house-key fallback → 1203/412), same AiUsageRecorder.checkBudget() BEFORE + record() AFTER, same configurable base-url kmosf.ai.anthropic.base-url (default real, → WireMock in tests), same x-api-key/anthropic-version POST + 1202/502 on non-2xx. Strict system prompt → ONLY JSON {name,phone,address,problem,urgency,callbackRequested}; defensive parse (extractJsonObject strips fences/prose; blank/parse-fail → VoicemailExtraction.empty(); never throws). Blank transcript short-circuits to empty without spend. Wired into TwilioVoicemailService.handleTranscript best-effort: extract(...).onErrorResume(→empty) so a 1200 budget / 1202 upstream NEVER drops the lead (AI is triage not truth, plan §8). EMPTY-DIFF vs main verified: AnthropicAiAssistService + AiUsageRecorder both 0-line diff (git diff main). | Error codes reused not re-allocated (1200/1201 budget gate, 1202 upstream, 1203 missing-key — all the AI range surfaced by the same call shape as the exemplar). |
| 1.4 lead (Contact find-or-create) + Activity(CALL,INBOUND) + notify Rob + auto-ack caller (best-effort) | done | (this commit) | BUILD SUCCESSFUL true-exit 0 (4s) | findOrCreateContact = EXPLICIT-BOOLEAN (findByTenantAndPhoneNumber.next().map(Optional::of).defaultIfEmpty(empty).flatMap(opt -> opt.isPresent() ? just : save)) — NOT switchIfEmpty(create); existing contact returned untouched (ServiceRequest-widget precedent); blank From → anonymous contact (lead never dropped). logCallActivity → UNCHANGED ActivityCrudService.create with type=CALL, direction=INBOUND, subjectType=CONTACT, subjectId=contact, summary=extraction.toSummaryLine(), body=raw transcript, payload={callSid,recordingSid,recordingUrl,fromNumber,extractedJson,transcriptionSource}. notifyRob best-effort (onErrorResume): EmailService.sendSingleEmail (from=kmosf.mail.smtp.username, to=conn.config.notifyEmail) + TwilioSmsService.sendSms (to=conn.config.notifyPhone) — targets from per-tenant IntegrationConnection.config, NOT hardcoded. autoAckCaller best-effort: TwilioSmsService.sendSms back to From (kmosf.voicemail.auto-ack-message). Ledger back-filled resolvedContactId+createdActivityId. Advisory VOICEMAIL_LEAD_CREATED emitted. §9 switchIfEmpty grep over integration/twilio/voice/: 2 operational hits — :203 genuine not-connected/4001 + :119 VoicemailExtractionService key-resolution house-key fallback (verbatim AnthropicAiAssistService:118 pattern); rest Javadoc/comments; ZERO switchIfEmpty(process/create). EMPTY-DIFF vs main (0 lines): AnthropicAiAssistService, AiUsageRecorder, TwilioSmsService, EmailService, ContactCrudService, ActivityCrudService, IntegrationConnection(+Service+Repository). | Contact find-or-create uses ContactRepository.save directly (the ServiceRequest-widget precedent), not ContactCrudService.create — keeps ContactCrudService empty-diff; both are valid intake paths. notify-from uses kmosf.mail.smtp.username (the house sender, the TemplatedEmailService precedent); notify TO targets are per-tenant config. |
| 1.5 BE ITs (signed callback / invalid-sig / duplicate CallSid / not-connected / TwiML voice) + TwilioRequestValidator unit test | done | db57a35 | compileJava+compileTestJava BUILD SUCCESSFUL true-exit 0; unit tests RUN locally GREEN (TwilioRequestValidatorTest 8/0, TwilioVoicemailControllerUrlTest 4/0); ITs written but NOT run locally (Docker daemon unreachable — exit 143 timeout) → rely on CI, mirror exemplars exactly | TwilioVoicemailIT mirrors DocumensoWebhookSignedIT/StripeWebhookIdempotencyIT/CalComWebhookIT exactly: @SpringBootTest(RANDOM_PORT)+@AutoConfigureWebTestClient+@Import(TestcontainersConfiguration)+@TestPropertySource job-disables; self-clean mongo.remove @BeforeEach; eventPublisher.stream() observer + Awaitility; mongo.findAll bypasses tenant scope. Cases: signedVoicemail→Contact+Activity(CALL,INBOUND)+notify(email+SMS)+auto-ack+1 ledger row+VOICEMAIL_*; invalidSignature→401/4000 zero-effect zero-WireMock zero-notify; duplicateCallSid→200 no-op exactly-1 Activity/ledger; notConnectedTenant→4001/404 zero-effect; voiceEndpoint→signed→<Record transcribe=true transcribeCallback playBeep> XML + invalid-sig→401/4000. §7: Anthropic→WireMock via @DynamicPropertySource(kmosf.ai.anthropic.base-url); TwilioSmsService+EmailService @MockitoBean (OnTheWayDispatchIT precedent — their base URLs not config-driven; mocking the seam = no live send + verifies dispatch). authToken/apiKey sandbox fakes. Deterministic signed URL via X-Forwarded-Proto/Host (port-independent). TwilioRequestValidatorTest = self-computed known-good sig (8 cases incl. tamper/sort/no-params). TwilioVoicemailControllerUrlTest = MockServerWebExchange reconstructFullUrl proof (no Docker). | Could not run ITs locally (no Docker); unit tests (validator sig + URL reconstruction — the two trickiest bits) run GREEN locally; ITs are exemplar-faithful for CI. 2 @MockitoBean (notify seams) = the OnTheWayDispatchIT precedent, not a Phase-F-style index-splinter @MockBean. |
| 1.6 docs in-PR (CLAUDE.md Phase-1 section + openapi.json) + final BE green + PR | done | b7ce150 | compileJava+compileTestJava BUILD SUCCESSFUL true-exit 0 | CLAUDE.md: new "Phase 1 — NMM voicemail-to-lead pipeline (SHIPPED — BE-only)" section in the Phase-H style (CalComWebhookService-mirror; verify-before-effect; explicit-boolean CallSid + ledger-insert-FIRST; transcription seam; AnthropicAiAssistService-mirror extraction; lead+Activity+notify; advisory events; module-gate deviation; error range 4000-4099; §7; reused-cores empty-diff; tests; BE-only). docs/api/openapi.json: the two new /public/integrations/twilio/{tenantId}/{voice,voicemail} POST paths hand-added (mirrors the committed Cal.com/Documenso webhook entries — tenantId path param, X-Twilio-Signature header, x-www-form-urlencoded body; /voice produces application/xml; /voicemail 200) because the Testcontainers OpenApiEndpointIT regenerator + the springdoc generateOpenApiDocs both need Docker/Mongo (neither reachable locally); CI's advisory verifyOpenApi refreshes the working copy. .claude/* NOT committed. | openapi.json hand-added (not generator-produced) — Docker daemon + local Mongo both unreachable, so neither the Testcontainers OpenApiEndpointIT export nor springdoc generateOpenApiDocs could run; the hand-added entries are structurally faithful to the committed Cal.com/Documenso webhook shapes and the new paths are present as required; CI regenerates the canonical form (verifyOpenApi is advisory in Phase A — won't fail the build; operationId drift gate deferred to Phase B). |

## No-live-external boundary (§7) — recorded
- [x] Twilio + Anthropic base URLs configurable and pointed at WireMock/mock in every test; auth token / api key are sandbox fakes; NO host hardcoded; NO live call/charge/send anywhere. — Anthropic base-url is kmosf.ai.anthropic.base-url (default real, → WireMock via @DynamicPropertySource in TwilioVoicemailIT); the VoicemailExtractionService reuses that exact property. The notify/auto-ack SMS + email go through the EXISTING TwilioSmsService/EmailService (whose base URLs are not config-driven — the documented OnTheWayDispatchIT limitation) → @MockitoBean'd in the IT (no live send; dispatch contract asserted). Twilio authToken + Anthropic apiKey are sandbox fakes in the IT. NO new live-STT outbound (transcription seam uses the inbound TranscriptionText; the Whisper impl is a documented default-OFF future seam). Provisioning a real Twilio number + call-forwarding is a SEPARATE HUMAN ACTION — never in this code/loop.

## External-ingress trust (the §9 #1 invariant) — recorded
- [x] verify-before-effect (X-Twilio-Signature via TwilioRequestValidator) precedes any effect; tenant from URL path NEVER payload; ledger-insert-FIRST (TwilioVoicemailEvent.save) under unique tenant_callsid_idx + DuplicateKeyException→Mono.empty(); synthetic TenantContext(...,INTEGRATION_TWILIO); duplicate redelivery → 200 no-op zero second effect. — verifiedConnection (TwilioRequestValidator.verify) precedes process() precedes voicemailEvents.save(ledger); controller resolves tenant from @PathVariable ONLY (4003 on bad UUID); ledger-insert-FIRST with onErrorResume(DuplicateKeyException→empty); synthetic INTEGRATION_TWILIO via contextWrite; CallSid idempotency proven by TwilioVoicemailIT.duplicateCallSid_200NoOp + invalidSignature_401_4000_zeroEffect + notConnectedTenant_4001_zeroEffect.
- [x] switchIfEmpty over the new package = genuine not-found / 4001 only; ZERO switchIfEmpty(process/create) on the idempotency seam. — grep over integration/twilio/voice/: 2 operational hits — TwilioVoicemailService:203 (genuine Twilio-not-connected/4001, the CalComWebhookService:105 mirror) + VoicemailExtractionService:119 (the verbatim AnthropicAiAssistService:118 key-resolution house-key fallback). CallSid idempotency + Contact find-or-create both explicit-boolean. ZERO switchIfEmpty(process/create).
- [x] Reused cores empty/additive diff: AnthropicAiAssistService / TwilioSmsService / EmailService / ContactCrudService / ActivityCrudService / IntegrationConnection*. — all 9 verified 0-line `git diff main` (AnthropicAiAssistService, AiUsageRecorder, TwilioSmsService, EmailService, ContactCrudService, ActivityCrudService, IntegrationConnection, IntegrationConnectionService, IntegrationConnectionRepository). Strictly-additive: ContactRepository.findByTenantAndPhoneNumber finder, TwilioVoicemailEvent ledger + repo, DomainEventType Phase-1 block, config props.
