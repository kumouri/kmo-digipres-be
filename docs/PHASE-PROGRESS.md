# Phase 3 (NMM AI intake) — Coverage-Window Automation — Progress Ledger

> Crash-recovery source of truth. Each sub-phase is its own commit; this row is set
> →in-progress (committed) as the first action and →done+results (committed) as the
> last action of every sub-phase. Plan: `i-had-an-idea-ticklish-rivest.md` §2 (Feature B /
> B2 tripwire), §4, §5 Phase 3, §6, §8. Builds on the just-merged Phase 1 (voicemail-to-lead)
> + Phase 2 (mole-photo triage) spine — the tripwire REUSES the Phase-2 pipeline
> (`MoleVisionService` classify + `FileStorageService` store + notify-Rob) and the UNCHANGED
> `MilestoneService.create` to drop a re-treatment Milestone on the customer's Project.

## Branch: nmm-ai-intake-phase-3-coverage-window  (base main @ d4361b8 — the merge that includes Phases 1+2)
## Model: Opus implementer = Opus 4.8 (1M context). BE-only — no FE this phase. No live external services (§7 — WireMock/mock/sandbox throughout).

| Sub-phase | Status | SHA | Build (compileJava+compileTestJava) | Mandated checks | Deviations |
|---|---|---|---|---|---|
| 3.1 error range 4013-4029 (carved from Phase-2's reserved 4013-4039) + DomainEventType Phase-3 block + mole-tripwire token type/service (HMAC, mirrors PublicWidgetTokenService but carries projectId) + admin token-issuer endpoint + additive nullable Project.coverageWindowEndsAt | done | (this commit) | compileJava BUILD SUCCESSFUL exit 0 | GlobalErrorHandler Javadoc: carved 4013-4029 = Phase 3 from Phase-2's reserved 4013-4039 (4030-4039 still reserved); 4013 tripwire widgetType mismatch (mirrors 4010/2700), 4014 no image part, 4015 unsupported media type, 4016 token's Project not found (defensive); 1600-1603 (tripwire-token rejections) + 1200-1203 (AI) + 1310/1311 (storage) + 2530-2532 (Twilio SMS) + 1300 (Activity) + 3410/3411 (UNCHANGED MilestoneService) + 1800 (RoleGuard) reused not re-allocated. DomainEventType +3 advisory constants (MOLE_TRIPWIRE_REPORTED / RETREATMENT_MILESTONE_CREATED / COVERAGE_NUDGE_SENT). MoleTripwireToken record {tenantId,widgetType,projectId,expiresAt} + MoleTripwireTokenService = NEW additive sibling of PublicWidgetTokenService (same base64url HMAC-SHA256 scheme + same kmosf.security.widget-token-secret; the ONE difference = a 4-field payload tenantId|widgetType|projectId|expiresAt carrying the Project id the 3-field Phase-2 token cannot hold — so PublicWidgetTokenService stays empty-diff). MoleTripwireTokenController (admin): POST /integrations/mole-tripwire/tokens/{projectId}, RoleGuard ADMIN + tenant-scoped Project load (4016 if absent) → issue token; @ConditionalOnProperty(kmosf.modules.mole-tripwire, matchIfMissing=true). Project.coverageWindowEndsAt = additive nullable Instant (the coverage-nudge selector; null on legacy/non-coverage Projects, no migration/index change; Project core otherwise untouched). | none |
| 3.2 mole-tripwire public report endpoint (MoleTripwireController, exchange.getMultipartData) + the MoleTripwireService pipeline (REUSE Phase-2: store Attachment → MoleVisionService classify → threshold; above-threshold mole → load Project from token → re-treatment Milestone via UNCHANGED MilestoneService.create + Activity + notify Rob + advisory events) | done | (this commit) | compileJava BUILD SUCCESSFUL exit 0 | MoleTripwireController: POST /public/integrations/mole-tripwire/{token}/report, reads multipart via exchange.getMultipartData() (NEVER @RequestBody MultiValueMap — the Phase-1 415 trap); image FilePart bytes joined via DataBufferUtils.join + release; optional `note` from FormFieldPart.value(); missing image part → 4014/400; @ConditionalOnProperty(kmosf.modules.mole-tripwire, matchIfMissing=true). MoleTripwireResponse (classification/confidence/aboveThreshold/message/attachmentId/retreatmentMilestoneId). MoleTripwireService (module-gated): MoleTripwireTokenService.verify (1600-1603 rejections; 4013 widgetType≠"mole-tripwire"; 4015 unsupported media AFTER token-verify so auth precedence holds) → synthetic TenantContext(tenantId,null,PUBLIC_WIDGET) via contextWrite (tenant + Project from the TOKEN only) → REUSE Phase-2 store (FileStorageService.putBytes("mole-tripwire",…) + Attachment(MOLE_PHOTO)) → REUSE UNCHANGED MoleVisionService.classify best-effort onErrorResume(→UNSURE) → threshold (kmosf.mole-tripwire.confidence-threshold default 0.6) → ABOVE: ProjectRepository.findByTenantIdAndId (4016 if absent) → re-treatment Milestone "Re-treatment — suspected new mole activity" (PENDING, due +7d) via UNCHANGED MilestoneService.create + Activity(NOTE, subjectType=CONTACT=project.primaryContactId, payload carries projectId/milestoneId — SubjectType untouched, no PROJECT value added) + best-effort notify Rob email+SMS to per-tenant Twilio config notifyEmail/notifyPhone (NOT hardcoded; onErrorResume) + RETREATMENT_MILESTONE_CREATED → return milestoneId; BELOW/UNSURE: record + notify "needs review", NO Milestone. MOLE_TRIPWIRE_REPORTED emitted on every report. NO idempotency ledger (per-customer report intentionally re-invocable — the Phase-2 MoleTriageService precedent). §9: the ONLY switchIfEmpty in the package = the Project not-found (4016) + MoleVisionService's reused key fallback; NEVER switchIfEmpty(create/process). | none — MoleTripwireService is one cohesive file so the report endpoint + the re-treatment path landed in one reviewable commit |
| 3.3 coverage-window check-in nudge job (CoverageNudgeJob @Scheduled, default-OFF, idempotent per (project,period) via explicit-boolean CoverageNudgeLog ledger probe + ledger-insert-FIRST) | done | (this commit) | compileJava BUILD SUCCESSFUL exit 0 | (B) SHIPPED (a clean single-additive-field selector was possible — no scope-question stop). CoverageNudgeJob: @Component @ConditionalOnProperty(prefix=kmosf.modules.coverage-nudge, name=enabled, matchIfMissing=FALSE) — DEFAULT-OFF (the ImapInboundPoller precedent; bean not created in CI/any default run → no live nudge sends → §7 satisfied by construction). @Scheduled(fixedDelay default 24h, initialDelay 60s) tick fire-and-forget subscribes on the scheduler thread (NEVER Netty; @EnableScheduling confirmed on KmoDigipresBeApplication); visible-for-test nudgeDueOnce() returns Mono<Void> the IT blocks (the OnTheWaySmsAutomation.seedAll() pattern). Selector = the single additive nullable Project.coverageWindowEndsAt via the STRICTLY-ADDITIVE ProjectRepository.findAllByTenantIdAndCoverageWindowEndsAtAfter (Mongo $gt skips null/legacy/non-coverage Projects; explicit tenantId predicate). Per active-coverage Project: compute periodKey (ISO week UTC) → EXPLICIT-BOOLEAN ledger probe (findBy…PeriodKey.map(true).defaultIfEmpty(false)) → if unseen: resolve customer phone (Project.primaryContactId → Contact.phones[].number) → ledger-insert-FIRST (CoverageNudgeLog unique tenant_project_period_idx, onErrorResume(DuplicateKeyException→empty) concurrent-rerun backstop) → send nudge via REUSED TwilioSmsService.sendSms → COVERAGE_NUDGE_SENT. NEVER switchIfEmpty(send/create). CoverageNudgeLog = system ledger (TenantScoped, NOT Auditable — the RecurringInvoiceOccurrence rationale) + CoverageNudgeLogRepository. Per-tenant + per-project onErrorResume so one failure never aborts the sweep. | none — clean minimal selector via ONE additive nullable Project field; ProjectRepository gains one strictly-additive derived finder (the Phase-1 ContactRepository.findByTenantAndPhoneNumber precedent), ProjectService untouched |
| 3.4 BE ITs (MoleTripwireIT report + Milestone; MoleTripwireTokenServiceTest; CoverageNudgeIT idempotent per period) + MoleTripwireItStorageTestConfig | done | (this commit) | compileTestJava BUILD SUCCESSFUL exit 0; ITs RUN GREEN (Docker up): MoleTripwireIT 6/0/0/0, MoleTripwireTokenServiceTest 5/0/0/0, CoverageNudgeIT 1/0/0/0, OpenApiEndpointIT 2/0/0/0 (fresh 02:42-02:43 timestamps) | MoleTripwireIT (6): valid token + above-threshold mole→200 + classification + Attachment(MOLE_PHOTO under tenants/<id>/) + RE-TREATMENT Milestone on the Project (PENDING, name contains "re-treatment", due set, id==resp.retreatmentMilestoneId) + Activity(NOTE,CONTACT=primaryContactId, payload has projectId+milestoneId) + notify(email→notifyEmail + SMS→notifyPhone) + WireMock hit + MOLE_TRIPWIRE_REPORTED + RETREATMENT_MILESTONE_CREATED; below-threshold unsure→200 aboveThreshold=false retreatmentMilestoneId=null NO Milestone (photo still stored); mole@0.40<0.6→NO Milestone; CORRECTLY-SIGNED wrong-widgetType token (built with the test secret, type="service-request")→401/4013 zero-effect zero-WireMock zero-notify; tampered token→401/1602 zero-effect; missing image→400/4014. MoleTripwireTokenServiceTest (5, no Docker): issue→verify round-trips tenant+Project+widgetType+expiry; tampered sig→1602; foreign secret→1602; expired→1603; malformed (no dot→1601, blank→1600). CoverageNudgeIT (1): nudgeDueOnce() nudges ONLY the active-coverage Project (window +30d) — the expired (−5d) + null-window Projects are skipped → exactly one SMS to the customer phone + one CoverageNudgeLog row (projectId, non-blank periodKey, tenantId) + one COVERAGE_NUDGE_SENT; a 2nd sweep same period → still exactly one SMS + one ledger row (idempotent per period). §7: Anthropic→WireMock @DynamicPropertySource; TwilioSmsService+EmailService @MockitoBean; FileStorageService = MoleTripwireItStorageTestConfig in-memory @Bean @Primary; nudge default-OFF, opted-in only in CoverageNudgeIT with the SMS seam mocked; secrets sandbox/test fakes. Shard-safe: precedented notify mocks + 1 WireMock @DynamicPropertySource + the precedented storage @Bean; self-clean mongo.remove; no application-test.properties/build.gradle shard change. | none |
| 3.5 docs in-PR (CLAUDE.md Phase-3 section + openapi.json regen) + final ITs GREEN + mark PR ready | done | (this commit) | (see 3.4 — final forced cleanTest re-run GREEN below) | CLAUDE.md Phase-3 section added (Phase-1/2 style, after the Phase-2 section). openapi.json regenerated via verifyOpenApi — both new tripwire paths present: /public/integrations/mole-tripwire/{token}/report + /integrations/mole-tripwire/tokens/{projectId}. §9 switchIfEmpty grep + reused-cores empty-diff vs d4361b8 re-confirmed (evidence sections below). PR #68 (draft, opened after 3.1 per the crash-hedge) marked ready. | none |

## No-live-external boundary (§7) — recorded
- [x] Anthropic base URL configurable and pointed at WireMock in every test; api key sandbox fake; widget-token-secret a test secret; NO host hardcoded; NO live charge/send/upload anywhere; nudge job default-OFF (no live sends in CI / any default run). — The Anthropic vision call is `kmosf.ai.anthropic.base-url` (→ WireMock via `@DynamicPropertySource` in MoleTripwireIT; the reused MoleVisionService reuses that exact property — empty-diff). The notify SMS + email go through the EXISTING TwilioSmsService/EmailService (base URLs not config-driven) → `@MockitoBean`'d in MoleTripwireIT (no live send; the OnTheWayDispatchIT precedent — dispatch contract asserted via captured to-targets). The S3 photo-store (`FileStorageService.putBytes`) is the in-memory `MoleTripwireItStorageTestConfig` `@Bean @Primary` stub (the MoleTriageItStorageTestConfig/ContractItStorageTestConfig precedent — no live bucket; NOT a `@MockBean` functional splinter). The CoverageNudgeJob is `@ConditionalOnProperty(...coverage-nudge.enabled, matchIfMissing=FALSE)` — DEFAULT-OFF, so the bean is not created in CI / any default run → ZERO live nudge sends; CoverageNudgeIT opts in explicitly AND `@MockitoBean`s TwilioSmsService. apiKey is a sandbox `sk-ant-test-…` fake; widget-token-secret is a test secret. NO host hardcoded; NO live charge/send/upload anywhere. Provisioning a live Anthropic key / real tripwire links texted to customers / nomomole.com CORS is a SEPARATE HUMAN ACTION — never in this code/loop.

## §9 reactive invariant — recorded
- [x] switchIfEmpty over the new moletripwire package(s) = genuine not-found only; ZERO switchIfEmpty(create/process/send). — grep over integration/moletripwire/ + the two MoleTripwire controllers: exactly TWO operational hits, both genuine not-found → 4016 (MoleTripwireService:259 the Project load; MoleTripwireTokenController:62 the admin Project load); the rest are Javadoc. The tripwire creates a Milestone (not a Contact — no find-or-create seam) and has NO idempotency ledger (a per-customer report is intentionally re-invocable — the Phase-2 MoleTriageService precedent — so no switchIfEmpty(process) on the tripwire at all). The CoverageNudgeJob idempotency seam is EXPLICIT-BOOLEAN (findBy…PeriodKey.map(true).defaultIfEmpty(false)) + ledger-insert-FIRST (CoverageNudgeLog unique tenant_project_period_idx, onErrorResume(DuplicateKeyException→empty) concurrent backstop), NEVER switchIfEmpty(send/create). Blocking I/O off the Netty loop: S3 putBytes = non-blocking S3AsyncClient; the base64 inside the reused MoleVisionService = Schedulers.boundedElastic(); the @Scheduled tick subscribes on the scheduler pool, never Netty.
- [x] Reused cores empty-diff vs the Phases-1+2-inclusive base (d4361b8 — the branch base; verified against d4361b8, NOT local `main`): MoleVisionService / MoleTriageService / ProjectService / MilestoneService / AnthropicAiAssistService / TwilioSmsService / EmailService / ActivityCrudService / FileStorageService / PublicWidgetTokenService / IntegrationConnection(+Repository) + all Phase-1/2 packages — `git diff d4361b8 --stat` over all of them = ZERO lines. Strictly-additive: the new integration/moletripwire/* package (MoleTripwireToken/TokenService/Service/Response + CoverageNudgeJob/Log/LogRepository), MoleTripwireController + MoleTripwireTokenController, the additive nullable Project.coverageWindowEndsAt field (+11 lines, no index/migration), the ONE additive ProjectRepository derived finder (+14 lines, the Phase-1 ContactRepository precedent; ProjectService untouched), the DomainEventType Phase-3 block (+3 constants), the GlobalErrorHandler 4013-4029 Javadoc, the config props, and the openapi.json regen. Full surface: 15 files, +1276/−31.

## Final forced clean re-run (the Phase-2 lesson — a cached re-run does not count) — recorded
A forced `cleanTest test --tests "*MoleTripwireIT" --tests "*MoleTripwireTokenServiceTest" --tests "*CoverageNudgeIT" --tests "*OpenApiEndpointIT"` re-run executed the ITs FRESH (not Gradle-up-to-date) → MoleTripwireIT 6/0, MoleTripwireTokenServiceTest 5/0, CoverageNudgeIT 1/0, OpenApiEndpointIT 2/0 GREEN. See the §7/§9 sections above for the evidence.

## Baseline note (IMPORTANT for the validator)
The branch is based on `d4361b8` (PR #67 merge — Phases-1+2-inclusive). Reused-cores empty-diff
MUST be verified against `d4361b8` (the correct base that includes Phases 1+2), NOT local `main`
(which may be behind that merge — a `git diff main` would falsely show Phase-1/2 files as new,
the Phase-2 lesson).

## Opus implementer note
Phase 3 implemented by Opus 4.8 (1M context) as a single supervised pass; Docker is up so the
ITs are run to GREEN locally before the PR is marked ready (the non-negotiable gate). Crash-hedge:
push + open a DRAFT PR as soon as 3.1 compiles, `git push` after EVERY sub-phase commit, mark the
PR ready only at the end. Each sub-phase: explicit `git add <paths>` (never -A/.; never the root
CLAUDE.md or `.claude/*`), compileJava+compileTestJava after each, full ITs to GREEN at the end.
