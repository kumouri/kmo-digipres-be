# Phase 2 (NMM AI intake) — "Is This a Mole?" Photo-Triage Pipeline — Progress Ledger

> Crash-recovery source of truth. Each sub-phase is its own commit; this row is set
> →in-progress (committed) as the first action and →done+results (committed) as the
> last action of every sub-phase. Plan: `i-had-an-idea-ticklish-rivest.md` §2 (Feature B),
> §4, §5 Phase 2, §6, §8. Builds on the just-merged Phase 1 (voicemail-to-lead) spine —
> the photo channel is a thin front end on the same shared intake/notify core, with a NEW
> additive `MoleVisionService` mirroring the Phase-1 `VoicemailExtractionService` /
> `AnthropicAiAssistService` exactly (but for vision).

## Branch: nmm-ai-intake-phase-2-photo-triage  (base main @ 9e8b2f4 — the merge that includes Phase 1)
## Model: Opus implementer = Opus 4.8 (1M context). BE-only — no FE this phase. No live external services (§7 — WireMock/mock/sandbox throughout).

| Sub-phase | Status | SHA | Build (compileJava+compileTestJava) | Mandated checks | Deviations |
|---|---|---|---|---|---|
| 2.1 error range 4010-4039 + DomainEventType Phase-2 block + MoleClassification value types + MoleVisionService core (vision, mirrors AnthropicAiAssistService) | done | fdba035 | compileJava BUILD SUCCESSFUL exit 0 | error-code block 4010-4039 added to GlobalErrorHandler Javadoc only (no handler code touched, no existing `<li>` modified): 4010 widget-type mismatch (mirrors 2700), 4011 no image part, 4012 unsupported media type; 1600-1603 (PublicWidgetTokenService token rejections) + 1200-1203 (AI) + 1310/1311 (storage) + 2530-2532 (Twilio SMS) + 1300 (Activity) reused not re-allocated. DomainEventType +2 advisory constants (MOLE_PHOTO_CLASSIFIED/MOLE_LEAD_CREATED) before private ctor. MoleClassificationCategory enum {MOLE,VOLE,GOPHER,NONE,UNSURE} + fromWire (case-insensitive, null/unrecognized → UNSURE, never throws — the adapter boundary). MoleClassification record {category,confidence,rationale} + unsure()/isPest()/toSummaryLine(). MoleVisionService = NEW additive sibling of AnthropicAiAssistService: same resolveKey (per-tenant IntegrationConnection(anthropic).secrets.apiKey → kmosf.ai.anthropic.house-key fallback → 1203/412), same AiUsageRecorder.checkBudget() BEFORE + record() AFTER, same configurable base-url kmosf.ai.anthropic.base-url (default real, → WireMock in tests), same x-api-key/anthropic-version POST + 1202/502 on non-2xx; the ONE difference = an image (base64) content block + a text block in the user message. Strict system prompt → ONLY JSON {classification,confidence,rationale}; defensive parse (extractJsonObject strips fences/prose; blank/parse-fail/unrecognized → MoleClassification.unsure(); clamps confidence to [0,1]; never throws). Base64 encode on Schedulers.boundedElastic() (§9/Safety — CPU-bound off the Netty loop). | none |
| 2.2 public multipart intake endpoint (MoleTriageController, exchange.getMultipartData) + MolePhotoIntakeParams + MoleTriageResponse + the complete MoleTriageService pipeline (token verify → store Attachment → classify → threshold → find-or-create Contact + Activity(NOTE) + best-effort notify Rob + advisory events → return classification) | done | ec722d1 | compileJava BUILD SUCCESSFUL exit 0 | MoleTriageController: POST /public/integrations/mole-triage/{token}/classify, reads multipart via exchange.getMultipartData() (NEVER @RequestBody MultiValueMap — the Phase-1 415 trap); image FilePart bytes joined via DataBufferUtils.join + release; optional name/phone/email/address from FormFieldPart.value(); missing image part → 4011/400; @ConditionalOnProperty(kmosf.modules.mole-triage, matchIfMissing=true). MolePhotoIntakeParams (trim-to-null text fields) + MoleTriageResponse (classification/confidence/aboveThreshold/message/attachmentId, triage-not-truth framing). MoleTriageService (module-gated): token verify via PublicWidgetTokenService.verify (1600-1603 rejections; 4010 widgetType≠"mole-triage"; 415→4012 unsupported media type AFTER token-verify so auth precedence holds) → synthetic TenantContext(tenantId,null,PUBLIC_WIDGET) via contextWrite (tenant from TOKEN only, never the payload — ServiceRequestWidgetController precedent) → FileStorageService.putBytes("mole-triage",…) + AttachmentRepository.save(subjectType="MOLE_PHOTO", auto-tenant-stamped) → MoleVisionService.classify best-effort onErrorResume(→UNSURE) → threshold (kmosf.mole-triage.confidence-threshold default 0.6; aboveThreshold = isPest && conf≥threshold) → find-or-create Contact EXPLICIT-BOOLEAN (phone→email→anonymous; Optional.isPresent() branch, NEVER switchIfEmpty(create); existing untouched) → Activity(NOTE, subjectType=CONTACT) via UNCHANGED ActivityCrudService.create → best-effort notify Rob email+SMS to per-tenant Twilio IntegrationConnection.config notifyEmail/notifyPhone (NOT hardcoded; onErrorResume) → advisory MOLE_PHOTO_CLASSIFIED + MOLE_LEAD_CREATED → return MoleTriageResponse. NO idempotency ledger (public classify intentionally re-invocable — ServiceRequestWidgetController has none either). | none — single cohesive service file, so the briefing's 2.2-skeleton + 2.3-lead-path landed in one reviewable commit |
| 2.3 (folded into 2.2 — the lead path is one cohesive MoleTriageService file) | done | (see 2.2) | — | — | — |
| 2.4 BE ITs (MoleVisionServiceIT WireMock-Anthropic; MoleTriageIT intake) + MoleTriageItStorageTestConfig | done | 8436df4 | compileJava+compileTestJava BUILD SUCCESSFUL exit 0; ITs RUN GREEN locally (Docker up): MoleVisionServiceIT 5/0/0/0, MoleTriageIT 7/0/0/0, OpenApiEndpointIT 2/0/0/0 (full context loads — no boot regression) | MoleVisionServiceIT (5): canned mole→parsed category/conf/rationale + image content block matchingJsonPath verified hit WireMock; prose/fenced JSON→still parsed (defensive); garbage→UNSURE never-throws; blank image→UNSURE zero-traffic; upstream 500→1202. MoleTriageIT (7): valid token + above-threshold mole→200 + classification returned + Attachment(MOLE_PHOTO, storageRef under tenants/<id>/) + Contact(mole-triage-lead tag, supplied phone) + Activity(NOTE,CONTACT) + notify(email→notifyEmail + SMS→notifyPhone) + WireMock hit + MOLE_PHOTO_CLASSIFIED + MOLE_LEAD_CREATED; below-threshold unsure→200 aboveThreshold=false "unclear" framing, still recorded; mole@0.40<0.6→aboveThreshold=false; wrong-widgetType token→401/4010 zero-effect zero-WireMock zero-notify; tampered token→401/1602 zero-effect; missing image part→400/4011; unsupported media type (pdf)→415/4012. §7: Anthropic→WireMock via @DynamicPropertySource(kmosf.ai.anthropic.base-url); TwilioSmsService+EmailService @MockitoBean (OnTheWayDispatchIT/Phase-1 precedent — base URLs not config-driven); FileStorageService = MoleTriageItStorageTestConfig in-memory @Bean @Primary (ContractItStorageTestConfig precedent — exercises putBytes without a live bucket, NOT a @MockBean splinter); apiKey/widget-token-secret sandbox/test fakes. Multipart POST via MultipartBodyBuilder (image ByteArrayResource + text parts). Shard-safe: 2 precedented notify mocks + 1 WireMock @DynamicPropertySource + the precedented storage @Bean; self-clean mongo.remove @BeforeEach; no application-test.properties/build.gradle shard change. | none |
| 2.5 docs in-PR (CLAUDE.md Phase-2 section + openapi.json regen) + final ITs GREEN + PR | done | (this commit) | `cleanTest test` forced re-run (Opus validator) BUILD SUCCESSFUL exit 0 — MoleTriageIT 7/0/0/0, MoleVisionServiceIT 5/0/0/0, OpenApiEndpointIT 2/0/0/0 (FRESH 20:41 timestamps, "2 executed" — not cached) | CLAUDE.md Phase-2 section present (Phase-H/Phase-1 style); openapi.json regen confirmed current (a `verifyOpenApi` re-run left it unchanged); §9 `switchIfEmpty` grep + reused-cores empty-diff vs 9e8b2f4 re-confirmed by the validator; PR opened. | implementer crashed (API 529 Overloaded) mid-2.5 — after committing 2.1/2.2/2.4 + the docs WIP, before committing docs or opening the PR — recovered per resume-interrupted-phase (see below). |

## No-live-external boundary (§7) — recorded
- [x] Anthropic base URL configurable and pointed at WireMock in every test; api key sandbox fake; widget-token-secret a test secret; NO host hardcoded; NO live charge/send/upload anywhere. — The Anthropic vision call is `kmosf.ai.anthropic.base-url` (default the real endpoint, → WireMock via `@DynamicPropertySource` in BOTH MoleVisionServiceIT and MoleTriageIT; MoleVisionService reuses that exact property — empty-diff from the Phase-1 VoicemailExtractionService approach). The notify SMS + email go through the EXISTING TwilioSmsService/EmailService (base URLs not config-driven — the documented OnTheWayDispatchIT limitation) → `@MockitoBean`'d in MoleTriageIT (no live send; dispatch contract asserted via captured to-targets). The S3 photo-store (`FileStorageService.putBytes`) is the in-memory `MoleTriageItStorageTestConfig` `@Bean @Primary` stub (the ContractItStorageTestConfig precedent — no live bucket; NOT a `@MockBean` functional splinter). apiKey is a sandbox `sk-ant-test-…` fake; widget-token-secret is a test secret. NO host hardcoded; NO live charge/send/upload anywhere in the loop. Provisioning a live Anthropic key / real public widget token / Cloudflare CORS for nomomole.com is a SEPARATE HUMAN ACTION — never in this code/loop.

## §9 reactive invariant — recorded
- [x] switchIfEmpty over the new molevision package = genuine not-found / key-resolution-fallback only; ZERO switchIfEmpty(create/process) on the find-or-create seam. — grep over integration/molevision/: exactly ONE operational hit — MoleVisionService:158 (the verbatim AnthropicAiAssistService:118 / VoicemailExtractionService:119 key-resolution house-key fallback). The Contact find-or-create seam in MoleTriageService is EXPLICIT-BOOLEAN (Optional.isPresent() branch over phone→email→anonymous), NEVER switchIfEmpty(create). There is NO idempotency ledger (a public classify is intentionally re-invocable — the ServiceRequestWidgetController has none), so no switchIfEmpty(process) risk at all. Blocking I/O off the Netty loop: S3 putBytes is the non-blocking S3AsyncClient; the base64 encode (inside MoleVisionService) is on Schedulers.boundedElastic().
- [x] Reused cores empty-diff vs the Phase-1-inclusive base (9e8b2f4 — the branch base; local `main` is BEHIND that merge so a `git diff main` falsely counts all of Phase 1 as new): AnthropicAiAssistService / AiUsageRecorder / TwilioSmsService / EmailService / ContactCrudService / ActivityCrudService / FileStorageService / S3FileStorageService / IntegrationConnection(+Service+Repository) / AttachmentService / AttachmentRepository / ContactRepository / PublicWidgetTokenService / Attachment / ALL integration/twilio/voice/* — verified `git diff 9e8b2f4 --stat` over all of them = ZERO lines. Phase 2 is strictly additive: the new integration/molevision/* package, controller/integration/MoleTriageController, the additive DomainEventType Phase-2 block (+2 constants), the additive GlobalErrorHandler 4010-4039 Javadoc `<li>`, and the openapi.json regen.

## Baseline note (IMPORTANT for the validator)
The branch is based on `9e8b2f4` (PR #64 merge — Phase-1-inclusive). The local `main` ref in this
worktree points at `2b24df8` (PR #63, PRE-Phase-1) and is BEHIND `9e8b2f4`; `origin/main` is
`5b9efa8`. Therefore reused-cores empty-diff was verified against `9e8b2f4` (the correct base that
includes Phase 1), NOT local `main`. A `git diff main` would falsely show all Phase-1 files
(ContactRepository phone finder, the whole integration/twilio/voice/ tree) as "new".

## Opus implementer note
Phase 2 implemented by Opus 4.8 (1M context) as a single supervised pass; Docker is up so the
ITs are run to GREEN locally before the PR (the non-negotiable gate — Phase 1's implementer
reported "complete" on a compile-only build whose ITs actually failed 6/6; this phase does not
repeat that). Each sub-phase: explicit `git add <paths>` (never -A/.; never the root CLAUDE.md
or `.claude/*`), compileJava+compileTestJava after each, full ITs to GREEN at the end.

## Opus orchestrator-validator recovery — recorded (2026-06-02; resume-interrupted-phase)
The Phase-2 implementer sub-agent died on an **API Error 529 (Overloaded)** with no report, after committing 2.1 (`fdba035`), 2.2 (`ec722d1`), 2.4 (`8436df4`) and leaving the 2.5 docs (the CLAUDE.md Phase-2 section + this ledger) as uncommitted WIP — it crashed before committing the docs or opening the PR. Per the `resume-interrupted-phase` runbook the orchestrator did NOT trust the (absent) summary and did NOT restart from zero: reconstructed from `git log` + this ledger, then **re-established the validation the crash ate** independently — `git status`/`diff` forensics confirmed the 3 commits + the WIP docs (no stash); a §9 `switchIfEmpty` grep over `integration/molevision/` (exactly one genuine `MoleVisionService:158` key-fallback hit; the rest Javadoc) + reused-cores empty-diff vs `9e8b2f4` (zero lines) re-confirmed the mandated checks; and — critically — a **forced `cleanTest test` re-run** (the first re-run was Gradle-"up-to-date"/cached, which does NOT count) executed the ITs fresh → MoleTriageIT 7/0, MoleVisionServiceIT 5/0, OpenApiEndpointIT 2/0 GREEN (fresh 20:41 timestamps). The uncommitted WIP docs were inspected and deliberately KEPT (accurate). Recovery was orchestrator-direct (the remainder — commit docs + open PR — was small + high-trust); no agent relaunch (the 529 was transient, but the work was already complete, so a relaunch was unnecessary). Friction logged via `log-friction`.
