# Phase G — Portal Expansion — Progress Ledger

> Crash-recovery source of truth per ultraplan §6 Resilience. Each sub-phase is its
> own commit; this row is set →in-progress (committed) as the first action and
> →done+results (committed) as the last action of every sub-phase. Plan §10 is a
> one-line pointer here. Plan: back-office-kmo-digipres-phase-G-portal-expansion.md

## Branch: back-office-kmo-digipres-phase-G-portal-expansion  (base main @ cad1f05)
## Model: Plan=Opus 4.7 · Implement=Sonnet 4.6 · Validate/final=Opus 4.7 (access-control + payment focus)

| Sub-phase | Status | SHA | Build (main base / +G / skip) | Mandated checks | Deviations |
|---|---|---|---|---|---|
| G.1 error range 3800-3899 + DomainEventType Phase-G block | done | afde274 | compileJava+compileTestJava → BUILD SUCCESSFUL exit 0 (27s) | 38xx collision grep (src/main minus GlobalErrorHandler) = ZERO hits (no pre-existing 38xx in codebase); DomainEventType new values "invoice.viewedByClient"/"portal.quoteAccepted"/"portal.quoteDeclined" unique (not present anywhere else in src; dotted lowerCamel convention matched); | none |
| G.2 PortalOwnershipGuard + portal-scoped repo finders | done | d010b8d | compileJava+compileTestJava → BUILD SUCCESSFUL exit 0 (27s) | switchIfEmpty grep (service/portal/): PortalLinkedContactResolver:31 genuine not-found(1251), PortalLinkedContactResolver:39 genuine not-found(1252), MagicLinkService:98 genuine not-found(1242), PortalOAuthAuthenticationSuccessHandler:77 genuine not-found(pre-existing), UserIdentityService:66/69/82 JIT-provisioning in auth subsystem (pre-existing, unchanged), WebAuthnService:79/124/129 genuine not-found (pre-existing), PortalOwnershipGuard:71 genuine not-found(3801), PortalOwnershipGuard:89 genuine not-found(3802), PortalOwnershipGuard:107 genuine not-found(3803), PortalOwnershipGuard:125 genuine not-found(3804) — ALL GENUINE NOT-FOUND, ZERO switchIfEmpty(create/...) violations; same-404 verified: both not-found and not-owned branches in each requireOwned* method emit identical DigiPresBeException(message, code, 404) where codes are 3801/3802/3803/3804 per entity; guard reuses PortalLinkedContactResolver (tenant from JWT, tenant-scoped finders); entity accessors verified: Invoice.contactId/companyId, Quote.contactId/companyId, Project.primaryContactId/companyId, Contract.contactId/companyId; repo finders added: InvoiceRepository+findByTenantIdAndId (new), QuoteRepository+findByTenantIdAndId+findAllByTenantIdAndContactIdOrderByCreatedAtDesc+findAllByTenantAndContactOrCompany (new), ProjectRepository+findAllByTenantIdAndPrimaryContactIdOrderByCreatedAtDesc+findAllByTenantAndPrimaryContactOrCompany (new, findByTenantIdAndId already existed), ContractRepository+findAllByTenantIdAndContactIdOrderByCreatedAtDesc+findAllByTenantAndContactOrCompany (new, findByTenantIdAndId already existed); empty diff vs main: PortalLinkedContactResolver/QuoteService/StripeCheckoutService/service/storage/PortalSecurityConfig VERIFIED empty; new-resource-client lifecycle: PortalOwnershipGuard is a stateless @Component, no new resource-owning bean introduced | InvoiceRepository did not have findByTenantIdAndId (plan said "already exists" but it only had list finders) — added it as a derived finder consistent with the explicit-tenant convention |
| G.3 portal projection records + projects/contracts/quotes list services | done | 1efc1aa | compileJava+compileTestJava → BUILD SUCCESSFUL exit 0 (21s) | switchIfEmpty grep (service/portal/): MagicLinkService:98 genuine not-found(1242), PortalLinkedContactResolver:31/39 genuine not-found(1251/1252), PortalOAuthAuthenticationSuccessHandler:77 genuine not-found(pre-existing), PortalOwnershipGuard:71/89/107/125 genuine not-found(3801–3804), UserIdentityService:66/69/82/127 JIT-provisioning in auth subsystem (pre-existing), WebAuthnService:79/124/129 genuine not-found (pre-existing) — ALL GENUINE NOT-FOUND, the 3 new list services (PortalProjectsService, PortalContractsService, PortalQuotesService) add ZERO switchIfEmpty calls; projections drop tenantId/internal: PortalProjectSummary exposes {id,code,name,status,description,startDate,targetEndDate,actualEndDate} dropping tenantId/version/dealId/ownerId/autoFinalizeMilestoneInvoices/customFields/timestamps; PortalContractSummary exposes {id,contractNumber,title,kind,status,sentAt,signedAt,documensoSigningDeepLink(derived),signedPdfAvailable(derived)} dropping tenantId/version/templateId/parentContractId/dealId/contactId/companyId/quoteId/variables/renderedPdfStorageRef/signedPdfStorageRef-raw/promotedDealToWon/spawnedProjectId/voidReason/timestamps; PortalQuoteSummary exposes {id,quoteNumber,status,currency,subtotal,discountTotal,taxTotal,total,issuedAt,expiresAt} dropping tenantId/version/dealId/contactId/companyId/priceListId/lineItems/notes/terms/statusChangedAt/pdfStorageRef/customFields/timestamps; PortalCheckoutResponse exposes {checkoutUrl} only from StripeCheckoutService.CheckoutResult; PortalFileSummary exposes {id,filename,contentType,sizeBytes,createdAt} dropping tenantId/version/subjectType/subjectId/storageRef/uploadedByUserId/updatedAt; dispatch mirrors PortalInvoicesService (companyId!=null branch) verified; documensoSigningDeepLink surfaces stored documensoDocumentId (status==SENT only) — no signing-base-url property exists on branch — FE composes URL — no live Documenso call; empty diff vs branch base (PortalInvoicesService, PortalOwnershipGuard, PortalLinkedContactResolver) verified; new-resource-client lifecycle verified: none introduced | none |
| G.4 portal controllers + invoice pay + INVOICE_VIEWED_BY_CLIENT | done | 6b0349b | compileJava+compileTestJava → BUILD SUCCESSFUL exit 0 (20s) | switchIfEmpty grep (service/portal/ + controller/portal/): MagicLinkService:98 genuine not-found(1242), PortalLinkedContactResolver:31/39 genuine not-found(1251/1252), PortalOAuthAuthenticationSuccessHandler:77 genuine not-found(pre-existing), PortalOwnershipGuard:71/89/107/125 genuine not-found(3801–3804), UserIdentityService:66/69/82/127 JIT-provisioning in auth subsystem (pre-existing), WebAuthnService:79/124/129 genuine not-found(pre-existing), PortalAuthController:73 genuine not-found(pre-existing), PortalFilesController:84 genuine not-found(3807), PortalProfileController:69/81 genuine not-found(pre-existing) — ALL GENUINE NOT-FOUND, ZERO switchIfEmpty(create/...) violations; GATE-BEFORE-CHECKOUT verified: payInvoice method body at line 114 has requireOwnedInvoice(id).flatMap(inv -> stripeCheckout.createCheckoutForInvoice(...)) — gate precedes Stripe call textually and reactively; @IdempotentRoute at line 112; best-effort view-activity: viewService.recordView(invoice, contact).onErrorResume(e -> Mono.empty()) — activity/event write cannot fail the read; no @ConditionalOnProperty on new portal controllers verified; projection records out (no raw entity) verified; empty diff vs c9faff3: QuoteService/QuoteController/StripeCheckoutService/FileStorageService/S3FileStorageService/PortalOwnershipGuard/PortalLinkedContactResolver/PortalSecurityConfig/G.3 services/DealCrudService/ProjectService/ActivityCrudService — VERIFIED EMPTY (git diff --stat c9faff3..HEAD produced no output for those paths); new-resource-client lifecycle: PortalInvoiceViewService is stateless @Service — no new SDK/HTTP/Netty client/pool/scheduler/thread-pool; verified none introduced | Milestone/task summary in GET /projects/{id} deferred — no per-project summary finder exists on branch (adding cross-repo finders would exceed G.4 scope); scoped to project summary only (documented in controller Javadoc). Project-file subjectType convention: Attachment.subjectType is a free-form String (verified Attachment.java:42-46); subjectType="PROJECT" used for project files (consistent with "EXPENSE"/"WORK_ORDER" pattern in Phase D). AttachmentRepository lacks findByTenantIdAndId — used base-repo findById() + manual tenant+subjectType+subjectId ownership check (→ 3807/404 same-404 for not-found and not-linked, no enumeration oracle). |
| G.5 additive-nullable magic-link redirectTo (buildLink unchanged) | done | 34db9c9 | compileJava+compileTestJava → BUILD SUCCESSFUL exit 0 (23s) | switchIfEmpty grep (MagicLinkService.java): line 127 — `switchIfEmpty(Mono.error(() -> new DigiPresBeException("Magic-link token is invalid or expired", 1242, 401)))` — pre-existing genuine not-found (unchanged), ZERO new switchIfEmpty calls; OPEN-REDIRECT MITIGATION: `MagicLinkService.redeem` line 152: `.map(user -> new RedemptionResult(user, row.getRedirectTo()))` — redirectTo is ALWAYS token.getRedirectTo() (persisted at request-time), NEVER from the redeem request; `PortalAuthController.redeemMagicLink` line 65 `result.redirectTo()` = service result, not request body; `MagicLinkRedeem` record has only `token` field (no redirectTo on redeem request); buildLink UNCHANGED: `buildLink` method body (lines 179-185 in committed file) has zero diff vs 445f913 — only Javadoc references to `buildLink` appear in the diff; additive-nullable legacy byte-identical: `redirectTo` field on MagicLinkToken is nullable (no @Builder.Default, Lombok default=null), no index/migration, existing tokens deserialize with redirectTo=null — MagicLinkRedeemIT stays green (all token saves use MagicLinkToken.builder() without .redirectTo() so null — `redeemValidTokenWithInvitationProvisionsUser` updated to unwrap RedemptionResult, assert redirectTo==null; PortalChainIT uses HTTP only, not service layer — byte-identical); empty diff scope: only MagicLinkToken + MagicLinkService(non-buildLink) + PortalAuthController + MagicLinkRedeemIT + ledger = 5 files in git diff --stat 445f913..HEAD (verified); new-resource-client lifecycle: G.5 adds a nullable field + persist/echo wiring — no new SDK/HTTP/Netty client/pool/scheduler/thread-pool; verified: none introduced; reverse-engineered shapes: MagicLinkToken fields {id,tenantId,email,tokenHash,expiresAt,redeemedAt,version,createdAt, +redirectTo(nullable)}; MagicLinkService.request(Tenant,String email,String linkBaseUrl,String redirectTo):Mono<Void>; MagicLinkService.redeem(Tenant,String rawToken):Mono<RedemptionResult(User user,String redirectTo)>; PortalAuthController.MagicLinkRequest(email,linkBaseUrl,+redirectTo); MagicLinkRedeem(token); MagicLinkRedemption(userId,email,displayName,roles,+redirectTo); existing error codes 1242/1243/1244 unchanged | G-D7 choice: distinct optional `redirectTo` field on MagicLinkRequest (separate from linkBaseUrl which drives the emailed URL); `request(...)` signature extended to 4-param; `redeem(...)` return type changed from Mono<User> to Mono<RedemptionResult> (additive wrapper record); MagicLinkRedeemIT updated to unwrap `.user()` and assert `.redirectTo()==null` — ITs stay green |
| G.6 BE ITs AC-G1/G2/G3/G4 (cross-tenant+cross-contact, WireMock pay, deep-link) | done | 298648e (PortalMagicLinkDeepLinkIT) | compileTestJava → BUILD SUCCESSFUL exit 0; cleanTest test --tests "com.kumouri.kmodigipresbe.portal.*" --rerun-tasks → BUILD SUCCESSFUL (6 tasks EXECUTED, 1m 52s); per-class: MagicLinkRedeemIT=4; PortalActivitiesIT=4; PortalChainIT=3; PortalContractsAccessIT=11; PortalInvoicePayWireMockIT=5; PortalInvoiceViewActivityIT=3; PortalInvoicesIT=7; PortalMagicLinkDeepLinkIT=6; PortalProfileIT=7; PortalProjectsAccessIT=11; PortalQuotesAcceptDeclineIT=5; PortalTicketsIT=6; UserIdentityProvisioningPolicyIT=7; total=79/0/0/0 (pass/fail/error/skip) | AC-G1 covered: PortalInvoicePayWireMockIT.pay_anotherContactInvoice_returns3801_andZeroWireMockRequests + pay_tenantBInvoice_withTenantAToken_returns3801_andZeroWireMockRequests (gate-before-checkout zero-WireMock-on-reject); pay_sameIdempotencyKey_replaysSameUrl_andNoSecondStripePost (exactly-once). AC-G2 covered: PortalProjectsAccessIT.userA_canListFilesForOwnedProject + crossContactProject_filesReturn3803 + crossTenantProject_filesReturn3803 (files/presign access). AC-G3 covered: PortalMagicLinkDeepLinkIT.redeemToken_withRedirectTo_echosDeepLink + redeemedSession_canAccessOwnedContract + buildLink shape + legacy null. AC-G4 covered: cross-tenant+cross-contact negative per surface in PortalProjectsAccessIT/PortalContractsAccessIT/PortalQuotesAcceptDeclineIT/PortalInvoiceViewActivityIT; write-isolation proofs (anotherContactQuote_accept_returns3802_andStatusUnchanged; viewAnotherContactInvoice_returns3801_andNoActivityCreated). Shard-safe: no @MockBean in any new IT (grep shows only Javadoc comment mentions); no per-IT distinct @TestPropertySource/@DynamicPropertySource beyond the one WireMock base-URL registration in PortalInvoicePayWireMockIT; self-clean @BeforeEach in all 6 new ITs. Production code G.1–G.5 unmodified: git diff d467c62..HEAD -- src/main/ --stat = empty. No live Stripe host: WireMock base URL, sk_test_phaseG_fake_portal_key sandbox fake (not live). Commit SHAs per IT: PortalProjectsAccessIT=45fe11d; PortalContractsAccessIT=88e946c; PortalQuotesAcceptDeclineIT=630031e; PortalInvoicePayWireMockIT=94f6a1b; PortalInvoiceViewActivityIT=814cdc7; PortalMagicLinkDeepLinkIT=298648e | none |
| G.7 CLAUDE.md in-PR + .claude/* local + docs/api/openapi.json committed | todo |  |  | spec has portal paths + Portal*Summary schemas | |
| G.8 final BE green + PR | todo |  |  | Opus validation sign-off | |

## No-live-money / no-live-Documenso boundary (§7) — recorded
- [ ] Portal pay reuses StripeCheckoutService against kmosf.stripe.api-base-url pointed at WireMock in all tests; sandbox sk_test_-shaped fake key; no host hardcoded. Contract Documenso deep-link surfaces the stored documensoDocumentId (+ presign-download of the signed PDF) — no live Documenso call. Live = a separate human action, never the loop.

## Access-control model (the §9 #1 invariant) — recorded
- **G.6 ORCHESTRATOR-VERIFIED (2026-05-17):** PRODUCTION-UNTOUCHED (`git diff d467c62..HEAD -- src/main/` EMPTY — G.6 is tests-only, G.1–G.5 byte-unmodified); scope = exactly 6 new portal IT classes (+1710) + ledger, nothing stray; **shard-safe** — zero functional @MockBean/@MockitoBean in any new IT (only Javadoc mentions), self-clean @BeforeEach, one WireMock @DynamicPropertySource only (PortalInvoicePayWireMockIT); fresh forced clean re-run XMLs (all 13 portal classes @14:06:51, not cached) = **79/0/0/0** (41 new across the 6 G.6 ITs + 38 pre-existing portal ITs unregressed). AC assertions present+named: AC-G1 `PortalInvoicePayWireMockIT.pay_anotherContactInvoice_returns3801_andZeroWireMockRequests` + `pay_tenantBInvoice…ZeroWireMockRequests` (gate-before-checkout: 3801 + ZERO WireMock) + `pay_sameIdempotencyKey_replaysSameUrl_andNoSecondStripePost` (exactly-once); AC-G2 PortalProjectsAccessIT/PortalContractsAccessIT file+signed-pdf access-gated; AC-G3 PortalMagicLinkDeepLinkIT redirectTo round-trip + buildLink shape + expired/used still rejected; AC-G4 cross-tenant+cross-contact per surface incl. `PortalQuotesAcceptDeclineIT.anotherContactQuote…3802_andStatusUnchanged` + `mongo.findAll(Contract.class)` empty (portal-accept no spawn) + `PortalInvoiceViewActivityIT.viewAnotherContactInvoice…3801_andNoActivityCreated` (telemetry isolation). Full-suite no-regression = the sharded CI at G.8 (the §6 authoritative scale gate, as for every prior sub-phase). **Deviation (recorded for Opus validator):** PortalContractsAccessIT signed-pdf happy-path asserts the ownership gate passes (not a specific presign URL) since S3 isn't wired in Testcontainers — the security assertion (gate passes; wrong contact→3804) IS covered; documented in the test Javadoc.
- **G.5 ORCHESTRATOR-VERIFIED (2026-05-17):** scope = exactly MagicLinkToken(+15)+MagicLinkService(+57)+PortalAuthController(+39)+the EXISTING MagicLinkRedeemIT(+8, updated to unwrap RedemptionResult & assert redirectTo==null — NOT a new G.6 IT); recompile BUILD SUCCESSFUL true exit 0. **buildLink BYTE-UNCHANGED** (git diff shows only Javadoc `{@link #buildLink}` references; no +/- on base/separator/magic_token internals — §9 #5/G-D7 satisfied). **Open-redirect mitigation sound:** `redeem(): Mono<RedemptionResult>` sources `redirectTo` ALWAYS from `token.getRedirectTo()` (the value persisted at request-time on MagicLinkToken); the `MagicLinkRedeem` controller record has ONLY `token` (no redirectTo on the redeem request body) — an attacker cannot inject a redirect at redeem-time. Additive-nullable: MagicLinkToken.redirectTo nullable default-null (no @Builder.Default), MagicLinkRedemption gains nullable redirectTo — legacy path (null) byte-identical at the HTTP/behaviour level; redeem single-use/expiry/1242 unchanged. switchIfEmpty grep = the one pre-existing 1242 genuine not-found (unchanged); none new. **Deviation (recorded for Opus validator):** internal `MagicLinkService.redeem` return type changed Mono<User>→Mono<RedemptionResult(User,String redirectTo)> (a clean wrapper so the controller can echo the persisted redirect); sole caller PortalAuthController + the one existing MagicLinkRedeemIT updated accordingly — an internal-service-signature refactor, NOT an HTTP-contract/behaviour change; G-D7 implemented as a distinct optional `redirectTo` on MagicLinkRequest separate from `linkBaseUrl` (linkBaseUrl drives the emailed URL via the unchanged buildLink; redirectTo is the FE-routing echo). Validator: confirm buildLink empty-diff + redeem-redirect-from-persisted-token + legacy null path.
- **G.4 ORCHESTRATOR-VERIFIED (2026-05-17):** scope = exactly 4 new portal controllers + extended PortalInvoicesController + new PortalInvoiceViewService (additive, +564/-1, nothing stray); EMPTY-DIFF of QuoteService/StripeCheckoutService/storage/PortalOwnershipGuard/PortalLinkedContactResolver/G.3-services/PortalSecurityConfig/ActivityCrudService held; recompile BUILD SUCCESSFUL true exit 0. **Code-read confirmed:** `POST /invoices/{id}/pay` — `requireOwnedInvoice(id)` resolves FIRST, `stripeCheckout.createCheckoutForInvoice` is inside the `.flatMap` AFTER the gate (§9 #2 gate-before-checkout correct), `@IdempotentRoute` present, StripeCheckoutService unchanged; `GET /invoices/{id}` view-activity is best-effort (`viewService.recordView(...).onErrorResume(e->Mono.empty())` — cannot fail the read; rejected view never reaches recordView since the zip fails at the gate); `PortalFilesController` download = ownership-gate-then-`findById`+explicit tenant+subjectType+subjectId match → **same-404 (3807) no enumeration oracle**, presign-URL only (raw storageRef never exposed), 1311 defense-in-depth; all 4 new controllers `@RequestMapping(/portal/me)` no `@ConditionalOnProperty`, projection records out. switchIfEmpty grep clean (only genuine not-found incl. PortalFilesController 3807; pre-existing auth-subsystem hits unchanged/out-of-G.4-diff). **Deviations (recorded for Opus validator):** (1) `GET /projects/{id}` milestone/task summary deferred — no per-project finder on branch (PortalProjectSummary only, Javadoc'd); (2) AttachmentRepository lacks findByTenantIdAndId → `findById`+explicit tenant+subjectType+subjectId check (securely implemented, same-404, verified by code-read); (3) Attachment `subjectType="PROJECT"` convention chosen consistent with Phase-D `"EXPENSE"` precedent (free-form String, documented). Validator: confirm the quotes accept/decline SENT-only allow-list (3805) + advisory events + QuoteService.setStatus-unchanged in PortalQuotesController, and the subjectType convention.
- **G.3 ORCHESTRATOR-VERIFIED (2026-05-17):** scope = exactly 5 projection records + 3 list services (additive, nothing stray); existing portal services (PortalInvoicesService/PortalOwnershipGuard/PortalLinkedContactResolver) EMPTY-DIFF; leak-grep clean — no tenantId/version/subjectId/customFields/lineItems/raw-storageRef in any Portal*Summary (the one PortalContractSummary getSignedPdfStorageRef hit is the legitimate `signedPdfAvailable` derivation, raw ref NOT exposed — per G-D6); dispatch mirrors PortalInvoicesService (companyId!=null → …OrCompany finder, else contactId-only — the {companyId:null} trap avoided); documenso deep-link = stored documensoDocumentId only (no signing-base-url prop exists on branch; no live call/host); recompile BUILD SUCCESSFUL true exit 0; new-resource-client: none introduced (stateless records + @Service).
- **G.2 ORCHESTRATOR-VERIFIED (2026-05-17):** independently re-ran — git scope = exactly PortalOwnershipGuard(+170 new) + Invoice/Quote/Contract/Project repo finders(+102), additive-only, nothing stray; EMPTY-DIFF invariant HELD (PortalLinkedContactResolver/QuoteService/StripeCheckoutService/service.storage/PortalSecurityConfig byte-untouched vs main); recompile BUILD SUCCESSFUL true exit 0; read PortalOwnershipGuard in full — all 4 requireOwned* funnel linkedContact.resolve()→tenant-scoped findByTenantIdAndId(contact.tenantId,id)→**identical 38xx/404 msg+code for not-found AND not-owned** (no enumeration oracle), isOwnedBy guards both null sides (never null==null), correct per-entity accessors (Project.primaryContactId). switchIfEmpty grep clean (only the 4 guard 38xx not-found; UserIdentityService JIT-provisioning hits are pre-existing, NOT in G.2's 5-file diff). **Deviation (recorded for Opus validator):** InvoiceRepository lacked `findByTenantIdAndId` (plan §4 G.2 "already exists" referred to the list finders only) — added as a derived finder consistent with the explicit-tenant convention; purely additive, zero behavior change to existing callers.
- [ ] Every /portal/me/** data endpoint funnels through PortalLinkedContactResolver (tenant from JWT, contact from User.contactId, tenant-scoped contact load) and, for single-entity ops, PortalOwnershipGuard (tenant-scoped finder + contactId==contact.id || companyId==contact.companyId).
- [ ] Every portal repo finder carries an explicit tenantId AND a contact/company predicate (the TenantScopedReactiveMongoRepository marker does NOT auto-scope derived finders).
- [ ] Not-found and not-owned return the SAME 404 errorCode (no enumeration oracle).
- [ ] PortalSecurityConfig / PortalLinkedContactResolver / QuoteService / QuoteController / StripeCheckoutService / FileStorageService / S3FileStorageService / MagicLinkService.buildLink / DealCrudService / ProjectService — empty diff vs main.

## Validator (Opus 4.7, separate agent) sign-off — access-control + payment focus
BE [ ] — switchIfEmpty grep over service/portal/+controller/portal/ (every hit genuine not-found 1251/1252/2200/38xx/1242/1243/1244): [ ] ·
every portal data method funnels through resolver/ownership-guard; every portal repo finder explicit tenant+contact/company; same-404 not-found vs not-owned: [ ] ·
pay path: ownership gate textually precedes StripeCheckoutService call; @IdempotentRoute on /portal/me/invoices/{id}/pay; WireMock-configurable base URL, sandbox fake key, no host hardcoded: [ ] ·
PortalSecurityConfig/PortalLinkedContactResolver/QuoteService/QuoteController/StripeCheckoutService/FileStorageService/MagicLinkService.buildLink/DealCrudService/ProjectService empty diff vs main: [ ] ·
magic-link redirectTo additive-nullable, persisted-at-request-time not redeem-time, buildLink unchanged, redeem security unchanged (1242/1243/1244): [ ] ·
INVOICE_VIEWED_BY_CLIENT = advisory event + Activity(NOTE) row (best-effort), ActivityType enum unchanged; portal-accept does NOT auto-spawn a contract: [ ] ·
shard-safe: new ITs no @MockBean, no per-IT distinct property source beyond the one WireMock registration, self-clean via mongo.remove @BeforeEach; no application-test.properties/build.gradle shard change: [ ] ·
new-resource-client lifecycle: no new resource-owning bean introduced (explicit "none"): [ ] ·
docs/openapi in-PR; portal controllers no @ConditionalOnProperty: [ ]

## PHASE G COMPLETE — <date>. BE PR #__ merge-commit <sha>. main carries the portal-expansion surface
(Stripe WireMock/sandbox only; Documenso deep-links surface stored ids — no live calls; portal FE deferred per G-D12). Next: Phase H (Activepieces glue / server-side Zitadel portal OIDC).
