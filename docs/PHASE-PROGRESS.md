# Phase H — External-Integrations-Glue — Progress Ledger

> Crash-recovery source of truth per ultraplan §6 Resilience. Each sub-phase is its
> own commit; this row is set →in-progress (committed) as the first action and
> →done+results (committed) as the last action of every sub-phase. Plan §10 is a
> one-line pointer here. Plan: back-office-kmo-digipres-phase-H-external-integrations-glue.md

## Branch: back-office-kmo-digipres-phase-H-external-integrations-glue  (base main @ 24f7e5e)
## Model: Plan=Opus 4.7 (orchestrator-direct) · Implement=Sonnet 4.6 · Validate/final=Opus 4.7 (external-ingress trust + auth-regression focus)

| Sub-phase | Status | SHA | Build (main base / +H / skip) | Mandated checks | Deviations |
|---|---|---|---|---|---|
| H.1 error range 3900-3999 + DomainEventType Phase-H block | done | f54b418 | compileJava+compileTestJava → BUILD SUCCESSFUL true-exit 0 (31s) | no error-code collision: grep \b39\d\d\b over src/main confirms only the new GlobalErrorHandler Javadoc (lines 199-218); CALCOM_BOOKING_SYNCED/CANCELLED unique — not present elsewhere in src; advisory only; Postmark-bounce reuses EMAIL_BOUNCED/EMAIL_SPAM; Activepieces seed + portal-Zitadel add no new event | none |
| H.2 Cal.com webhook + client + Meeting source-of-truth projection | done | 83b7441 | compileJava+compileTestJava → BUILD SUCCESSFUL true-exit 0 (16s) | switchIfEmpty grep integration/calcom/: 1 hit — CalComWebhookService.java:105 `.switchIfEmpty(Mono.error(()-> new DigiPresBeException("Cal.com is not connected for this tenant", 3901, 404)))` — genuine not-connected/3901 provider-not-connected ONLY; ZERO switchIfEmpty(process/create/reconcile). verify-before-effect: `CalComSignatureVerifier.verify(:119)` precedes `process(:123)` precedes `calComEvents.save(ledger)(:192)`. tenant from URL path: controller `@PostMapping("/{tenantId}/webhook") public Mono<Void> webhook(@PathVariable String tenantId, ...)` — NEVER payload. ledger-insert-FIRST: `calComEvents.save(ledger).onErrorResume(DuplicateKeyException.class, e -> Mono.empty())` at :192-196 before Meeting upsert at :203+; unique tenant_event_idx on (tenantId,calComEventId) in CalComWebhookEvent.java. empty diff vs main: MeetingCrudService/BookingService/BookingLinkService/ActivityCrudService/integration.stripe/DocumensoWebhookService = EMPTY (verified). Meeting.java additive-only: +calComBookingUid (nullable String, @Indexed sparse=true) only. new-resource-client lifecycle: verified — CalComClient uses the shared WebClient.Builder bean; no new SDK/HTTP/Netty/pool/scheduler/thread-pool bean introduced. no live Cal.com: apiBaseUrl default https://api.cal.invalid (.invalid non-routable), configurable (kmosf.calcom.api-base-url); signing secrets are sandbox/test values only; no host hardcoded. | Cal.com signing scheme assumption: simple HMAC-SHA256 hex digest in X-Cal-Signature-256 (with optional sha256= prefix), isolated to CalComSignatureVerifier only + CalComEventAdapter.parse payload shape assumption. Both isolated to ≤3 files per F-D7 adapter-boundary discipline. Recorded in plan H.2 Deviations. |
| H.3 IMAP inbound poll-source → unchanged InboundEmailService | done | 51fee42 | compileJava+compileTestJava → BUILD SUCCESSFUL true-exit 0 (26s) | switchIfEmpty grep integration/imap/: 2 hits — both Javadoc comments (line 59: `NEVER {@code switchIfEmpty(ingest)}`, line 245: `NEVER switchIfEmpty(ingest)`); ZERO switchIfEmpty(ingest/process/create) in operational code. default-OFF: `@ConditionalOnProperty(prefix = "kmosf.imap.inbound", name = "enabled", matchIfMissing = false)` at ImapInboundPoller.java:72. Message-ID idempotency: explicit-boolean skip at :249-258 — `messages.findFirstByTenantIdAndMessageId(ctx.tenantId(), rfcMessageId).map(x -> true).defaultIfEmpty(false).flatMap(seen -> seen ? Mono.empty() : ingestParsed(parsed))`. empty diff vs main: InboundEmailService/InboxThread/AngusConfig = EMPTY (verified git diff 24f7e5e..HEAD); InboxMessage = additive-only (+@Indexed(sparse=true) annotation + Javadoc on pre-existing messageId field; no field added/removed). new-resource-client lifecycle: verified — IMAP Store opened+closed per poll cycle (fetchUnseenMessages try/finally closeQuietly(folder/store)), no long-lived connection bean, no new SDK/pool/scheduler/thread-pool bean. no live IMAP: ImapProperties host default `imap.mail.invalid` (.invalid non-routable), configurable (kmosf.imap.inbound.host), default-OFF (matchIfMissing=false), no host hardcoded. | InboundEmailService ingest entrypoint reused: `ingest(InboundEmail)` at :41 — takes InboundEmail record (contactId,messageId,from,to,subject,htmlBody,textBody,receivedAt); poller calls this unchanged. InboxMessage.messageId field pre-existed (:35); @Indexed(sparse=true) is the additive-only H.3 change (no @Builder.Default; nullable; backward-compatible). ImapInboundPoller.processMessage requires active TenantContext from caller — this is documented in pollOnce() Javadoc; the H.7 IT must inject context via contextWrite. No deviations from plan scope. |
| H.4 Postmark bounce/spam routing (in-place) | done | d82dd85 | compileJava+compileTestJava → BUILD SUCCESSFUL true-exit 0 (31s) | switchIfEmpty grep integration/postmark/: 3 hits — (1) PostmarkWebhookService.java:67 `.switchIfEmpty(Mono.error(() -> new DigiPresBeException("Postmark is not connected for this tenant", 1702, 404)))` — genuine not-connected/provider-not-connected ONLY; (2) :148 Javadoc comment `NEVER {@code switchIfEmpty(process)}` — documentation; (3) :171 code comment `// NEVER switchIfEmpty(process) — the §9 invariant` — documentation. ZERO switchIfEmpty(process/create/record). empty diff vs main (24f7e5e): PostmarkAuthVerifier = EMPTY (verified git diff 24f7e5e..HEAD — no output); PostmarkWebhookController = EMPTY (verified); DomainEventType = 16-line diff from H.1 only (CALCOM_BOOKING_SYNCED/CANCELLED) — H.4 adds NO new DomainEventType constant, reuses existing EMAIL_BOUNCED/EMAIL_SPAM. PostmarkWebhookService additive-only: H.4 diff (9a19f7a..HEAD) = imports + ContactRepository field + 9-line if-branch in process() + 108-line processDeliverabilityEvent() + recordDeliverabilityAndPublish() added ONLY; existing process() Delivery/Open/Click path (EmailEngagement entity builder through .contextWrite) byte-unchanged; mapRecordType() and domainEventTypeOf() byte-unchanged. idempotency MessageID+RecordType explicit-boolean: `Mono<Boolean> seenProbe = messageId != null ? engagements.findFirstByTenantIdAndMessageIdAndEvent(tenantId, messageId, event).map(existing -> true).defaultIfEmpty(false) : Mono.just(false); return seenProbe.flatMap(seen -> { if (seen) { return Mono.empty(); } return recordDeliverabilityAndPublish(...); })` — explicit-boolean, never switchIfEmpty(process). deliverability flag additive-nullable: `private EmailDeliverabilityStatus emailDeliverability;` on Contact.java — no @Builder.Default, nullable, no existing field changed. reuse existing events: `events.publish(DomainEvent.of(domainEventTypeOf(event), tenantId, saved.getId(), domainPayload))` — domainEventTypeOf() returns EMAIL_BOUNCED (for BOUNCE) / EMAIL_SPAM (for SPAM) — existing constants from Phase 9c, no new DomainEventType. | RecordType dispatch seam: the existing process() mapRecordType() switch already mapped Bounce→BOUNCE and SpamComplaint→SPAM; H.4 adds an in-place early-return branch before the existing engagement-save code so that BOUNCE/SPAM route to the new idempotency+flag path while Delivery/Open/Click continue through the existing byte-unchanged path. Contact flag placement: additive-nullable emailDeliverability on Contact.java (matching the Phase-E paymentTerms precedent — nullable, no @Builder.Default); new EmailDeliverabilityStatus enum. No unique ledger index for bounce (the EmailEngagement already has tenant_message_idx; the idempotency probe uses findFirstByTenantIdAndMessageIdAndEvent which scopes on event too, sufficient for the 200 no-op guarantee). |
| H.5 Activepieces webhook-subscription seed | done | 6b6a53c | compileJava+compileTestJava → BUILD SUCCESSFUL true-exit 0 (25s) | switchIfEmpty grep integration/activepieces/: 5 hits — all Javadoc/code comments documenting what NOT to do (`NEVER switchIfEmpty(create)`) — ZERO operational switchIfEmpty(create). empty diff vs main (24f7e5e): WebhookDeliveryService = EMPTY (verified); WebhookSubscription = EMPTY (verified). WebhookSubscriptionRepository = one additive derived finder `findFirstByTenantIdAndUrlAndEventTypesContaining(UUID tenantId, String url, String eventType)` only (no existing method changed). @IdempotentRoute present on seed POST at ActivepiecesController.java:84. @ConditionalOnProperty(prefix = "kmosf.modules.activepieces", name = "enabled", matchIfMissing = true) present at ActivepiecesController.java:65. seed idempotency explicit-boolean: `subs.findFirstByTenantIdAndUrlAndEventTypesContaining(tenantId, url, eventType).map(x -> true).defaultIfEmpty(false).flatMap(seen -> seen ? <return existing / no-op> : <create>)` — NEVER switchIfEmpty(create). R8 read-only token: Javadoc `@param capabilityToken: MUST NOT be an Activepieces admin-scope token (R8 — unconditional)` + code comment `// R8: store ONLY the read-only delivery/capability token. An Activepieces admin-scope token MUST NEVER be stored here.` stored as WebhookSubscription.secret. new-resource-client lifecycle: verified — reuses existing WebhookDeliveryService (its own WebClient/breaker); no new SDK/HTTP/Netty/pool/scheduler/thread-pool bean introduced. no live Activepieces: seed persists WebhookSubscription row only; no live HTTP call at seed time; no config base URL hardcoded (only timeout default; target URL supplied per-request). | ActivepiecesProperties carries no base URL (no live host ever hardcoded). Reuse seam: WebhookSubscription.url stores the Activepieces webhook-trigger URL; WebhookSubscription.secret stores the read-only capability token; WebhookSubscription.eventTypes = [DomainEventType.INVOICE_FINALIZED] by default. One additive derived finder `findFirstByTenantIdAndUrlAndEventTypesContaining` added to WebhookSubscriptionRepository for the explicit-boolean exists-probe. No deviations. |
| H.6 opt-in portal Zitadel oauth2Login (reuse A2) | done | 49df0e6 | compileJava+compileTestJava → BUILD SUCCESSFUL true-exit 0 (22s) | switchIfEmpty grep H.6 portal/auth files: 3 hits — (1) PortalZitadelSuccessHandler.java:118 `tenants.findById(ctx.tenantId()).switchIfEmpty(Mono.error(3930,404))` — genuine not-found: tenant from ZitadelOrgTenantCache resolution not in DB; 3930-style ONLY. (2) PortalZitadelSuccessHandler.java:144 `users.findById(ctx.userId()).switchIfEmpty(Mono.error(3931,401))` — genuine not-found: JIT-provisioned user ID not found post-provision; 3931-style ONLY. (3) PortalOAuthAuthenticationSuccessHandler.java:97 `tenants.findById(tenantId).switchIfEmpty(Mono.error(1232,400))` — PRE-EXISTING Google/Microsoft path, UNCHANGED. ZERO switchIfEmpty(provision/create). empty diff vs main: JwtTenantResolver/ZitadelClaimTenantResolver/ZitadelOrgTenantCache/MagicLinkService = EMPTY (verified git diff 24f7e5e..HEAD — no output). PortalSecurityConfig additive-only: diff adds zitadelEnabled field to OAuthRegistrationsSummary record + anyEnabled() expression only; existing matchers/permitAll/authenticated/oauth2ResourceServer/oauth2Login wiring lines are UNTOUCHED. Non-opted byte-identical: non-opted tenant user (zitadelOrgId==null) never navigates to /portal/auth/oauth2/authorization/zitadel; the permitAll list + anyExchange().authenticated() + oauth2ResourceServer JWT wiring = unchanged lines (not in diff). A2 reuse verbatim: PortalZitadelSuccessHandler calls ZitadelClaimTenantResolver.resolve(jwt):113 (unchanged) which internally calls ZitadelOrgTenantCache.resolve(orgId) + RoleClaimMapper.mapRoles(jwt) + ZitadelJustInTimeUserProvisioner.provision(...) — all four empty-diff vs 24f7e5e. opt-in gate: if (tenant.getZitadelOrgId() == null) → DigiPresBeException(3930,404) at PortalZitadelSuccessHandler.java:121-126. new-resource-client lifecycle: verified — no new HTTP/WebClient/Netty/pool/scheduler/thread-pool bean; PortalZitadelSuccessHandler wires only already-managed Spring beans. no live Zitadel: issuerUri=${KMOSF_OAUTH_ZITADEL_PORTAL_ISSUER:} (empty default); registration created only when client-id+client-secret non-blank; non-routable .invalid fallback in code; no Zitadel registration in dev/test. | OAuthRegistrationsSummary record constructor change (2→3 params): additive record field; OAuthClientRegistrationConfig.oauthRegistrationsSummary bean updated to pass zitadel boolean — strictly additive. PortalProperties.Oauth record: zitadel Provider field added (additive nullable). PortalOAuthAuthenticationSuccessHandler: additive early-return branch for registrationId=="zitadel" delegates to PortalZitadelSuccessHandler; Google/Microsoft branches byte-identical. |
| H.7 BE ITs AC-H1/H2/H3/H4/H5 (WireMock/stub, shard-safe) | done | def14f9 | ./gradlew cleanTest test --tests 5 new ITs → PASS (CalComWebhookIT 5/0/0/0, ImapInboundPollIT 3/0/0/0, PostmarkBounceIT 5/0/0/0, ActivepiecesSeedIT 3/0/0/0, PortalZitadelFederationIT 3/0/0/0; forced-clean re-run identical); integration+portal packages 21 classes all green | production-untouched: git diff 49df0e6..HEAD --stat -- src/main/ = EMPTY; shard-safe: zero functional @MockBean in new ITs, self-clean mongo.remove @BeforeEach on all touched collections, application-test.properties+build.gradle diff = EMPTY; AC-H1: bookingCreated_meetingUpsertedActivityCreatedEventObserved + bookingCancelled_projectionCancelledEventObserved; AC-H5(calcom): duplicateEventId_200NoOp_zeroSecondEffect + invalidSignature_401_3900_zeroEffect + notConnectedTenant_3901_zeroEffect; AC-H2: ingest_knownSender_createsThreadAndMessage + ingest_duplicateMessageId_onlyOneMessagePersisted + ingest_unknownSender_threadCreatedUnclaimed; AC-H3: seed_exactlyOneSubscriptionRow_capabilityTokenStoredAsSecret + seed_idempotent_secondCallNoSecondRow + invoiceFinalized_webhookDeliveryServicePostsToWireMock; AC-H4: optedTenant_zitadelClientJwt_portalAuthSucceedsAndJitCreatesUser + nonOptedTenantOrgClaim_rejectedAtZitadelOrgCache_3301_noSession + nonOptedTenant_magicLinkPathStillPermitted_byteIdentical; AC-H5(postmark): bounce_contactFlaggedBounced_emailBouncedEventObserved + spamComplaint_contactFlaggedSpam_emailSpamEventObserved + duplicateBounce_idempotent200NoOp_zeroSecondEngagementRow + wrongBasicAuth_401_zeroEffect + openWebhook_stillRecordedAsBeforeNoRegression | Deviations: ImapInboundPollIT drives InboundEmailService.ingest() directly (not the IMAP Store/Session I/O layer) — necessary because ImapInboundPoller creates jakarta.mail.Store internally from Session, not via DI; the poller bean IS instantiated in the context (kmosf.imap.inbound.enabled=true). PortalZitadelFederationIT drives Bearer JWT path via ZitadelClaimTenantResolver (existing A2 path) — not the OAuth2 server-side redirect flow (which requires browser round-trip). Both are the correct testable shape for the unchanged production code. Exemplars mirrored verbatim: DocumensoWebhookSignedIT (signed-webhook inbound shape), StripeCheckoutWireMockIT (WireMock outbound shape), PortalZitadelOptInIT + ZitadelFederationIT (A2 mock-OIDC harness); no new harness built. |
| H.8 docs in-PR + openapi.json + final BE green + PR | done | e11596f (in-progress), 586d3f3 (CLAUDE.md wip checkpoint), d0f2082 (openapi.json + note) | ./gradlew --no-daemon test --tests "*OpenApiEndpointIT" verifyOpenApi → BUILD SUCCESSFUL true-exit 0 (1m 2s) | spec has /public/integrations/calcom/{tenantId}/webhook + /api/v1/integrations/activepieces/seed paths; CLAUDE.md Phase-H SHIPPED note added (Cal.com ADR + StripeWebhookService-mirror pattern + IMAP default-OFF + Postmark in-place + Activepieces R8 seed + portal-Zitadel opt-in + error range 3900-3999 + §7 no-live-external + BE-only + Phase-I pause); .claude/* NOT committed — git diff 34bf7db..HEAD shows ONLY CLAUDE.md + docs/api/openapi.json + docs/PHASE-PROGRESS.md (no .claude, no src); production+tests frozen: git diff 0be585f..HEAD --stat -- src/ = EMPTY | Orchestrator flags #3 (ImapInboundPollIT test depth) and #4 (PortalZitadelFederationIT test depth) inherited from H.7 — both escalated to the Opus validator for adjudication; postmark bounce idempotency (H.4 orchestrator flag) escalated to the Opus validator; none of these block the docs sub-phase |

## No-live-external boundary (§7) — recorded
- [x] Cal.com/IMAP/Mailcow/Activepieces/Postmark/Zitadel = WireMock/sandbox/stub/.invalid-default, configurable base URLs, default-OFF pollers; no host hardcoded, no live token/charge/deployment. Live = a separate human action, never the loop. — **Opus-validated PASS (PR #62, §9.6): CalComProperties default api.cal.invalid, ImapProperties imap.mail.invalid, Activepieces no base URL, Zitadel issuer empty-default + zitadel.internal.invalid fallback gated on non-blank creds; pollers default-OFF; no host/token/charge hardcoded.**

## External-ingress trust (the §9 #1 invariant) — recorded
- [x] Every new webhook: verify signature/auth BEFORE any effect; tenant from URL path NEVER payload; ledger-insert-FIRST + unique tenant_event_idx + DuplicateKeyException→Mono.empty(); synthetic TenantContext(...,INTEGRATION_*); duplicate redelivery → 200 no-op zero second effect. — **Opus-validated PASS (§9.2): Cal.com verify(:119)→process(:123)→ledger-insert-FIRST(:192-197 DuplicateKey→empty)→synthetic INTEGRATION_CALCOM; tenant @PathVariable only; CalComWebhookIT proves invalid-sig-401 / not-connected-3901 / dup-200-no-op.**
- [x] switchIfEmpty over each new integration/<provider>/ = genuine not-found / 2510 only; ZERO switchIfEmpty(process/create). — **Opus-validated PASS (§9.1): only genuine not-found (3901/1702/3930/3931 + Javadoc/comment mentions); ZERO switchIfEmpty(process/create/ingest/reconcile/provision) in any new code.**
- [x] Reused cores empty/additive diff: InboundEmailService / WebhookDeliveryService / WebhookSubscription / PostmarkAuthVerifier / A2 JwtTenantResolver+ZitadelClaimTenantResolver+ZitadelOrgTenantCache / MeetingCrudService / BookingService / StripeWebhookService / DocumensoWebhookService / PortalSecurityConfig(local) / MagicLinkService.buildLink. — **Opus-validated PASS (§9.3): all 13 reused cores 0-line diff vs 24f7e5e (programmatic); strictly-additive items (repo finders, nullable fields, Phase-H DomainEventType block, PortalSecurityConfig record param) are plan-anticipated, not violations.**
- [x] Portal Zitadel federation opt-in per Tenant.zitadelOrgId; non-opted path byte-identical. — **Opus-validated PASS (§9.5): opt-in 3930 gate before any session mint, fail-closed; non-opted local magic-link/passkey byte-identical (SecurityWebFilterChain bean unchanged; the only reachable change is the additive zitadel early-return a non-opted user never triggers); A2 reused verbatim; buildLink 0-line diff.**

## Orchestrator between-chunk verification — recorded (trust-but-verify vs durable state, not the agent summary)
- **H.1 ORCHESTRATOR-VERIFIED:** GlobalErrorHandler Javadoc-only 3900-3999 block (no existing `<li>` touched, no handler code); DomainEventType +2 advisory constants before private ctor; collision grep `\b39\d\d\b` clean; independent compileJava+compileTestJava BUILD SUCCESSFUL true exit 0. SHA f54b418.
- **H.2 ORCHESTRATOR-VERIFIED (deep read — the ingress-trust chokepoint):** CalComWebhookController tenant from @PathVariable ONLY (never payload), /public/**, no @ConditionalOnProperty; handle() = CalComSignatureVerifier.verify gates entry to process() (verify-before-effect, textually+reactively); event-id idempotency = explicit-boolean probe (NOT switchIfEmpty); processAndRecord = ledger-insert-FIRST under unique tenant_event_idx + DuplicateKeyException→Mono.empty(), then synthetic TenantContext(...,INTEGRATION_CALCOM); reused cores (MeetingCrudService/BookingService/BookingLinkService/ActivityCrudService/integration.stripe/DocumensoWebhookService) EMPTY-DIFF (programmatic); Meeting additive-only (@Indexed(sparse=true) calComBookingUid); compile green. Deviations (F-D7 adapter-boundary): Cal.com sig scheme isolated to CalComSignatureVerifier, payload to CalComEventAdapter; 3901 used for both not-connected & secret-missing. SHA 83b7441. All within plan discipline.
- **H.3 ORCHESTRATOR-VERIFIED:** InboundEmailService/InboxThread/AngusConfig/integration.postmark EMPTY-DIFF (programmatic); InboxMessage additive-only (messageId field PRE-EXISTED — only +@Indexed(sparse=true)+Javadoc); idempotency = explicit-boolean tenant-scoped Message-ID probe (read in code, not switchIfEmpty(ingest)); @ConditionalOnProperty(...matchIfMissing=false) default-OFF + Spring @Scheduled posture confirmed; per-cycle store open/close; compile green. SHA 51fee42. No deviations.
- **H.4 ORCHESTRATOR-VERIFIED + DEVIATION FLAGGED FOR THE H.8 VALIDATOR:** PostmarkAuthVerifier/PostmarkWebhookController/DomainEventType EMPTY-DIFF (programmatic — reuses existing EMAIL_BOUNCED/EMAIL_SPAM, no new event/controller/auth change); PostmarkWebhookService in-place ADDITIVE-ONLY (existing Delivery/Open/Click path + mapRecordType/domainEventTypeOf byte-unchanged; only a 9-line BOUNCE/SPAM early-return guard + 2 new private methods); Contact +1 additive-nullable emailDeliverability (+ new EmailDeliverabilityStatus enum) + additive EmailEngagementRepository finder; switchIfEmpty grep clean (only :67 genuine 1702 not-connected); compile green. SHA d82dd85. **⚠ ORCHESTRATOR FLAG (the separate Opus validator at H.8 MUST adjudicate, do NOT accept the agent's self-assessed "sufficient"):** H.4's bounce/spam idempotency is *probe-then-act* on the existing EmailEngagement collection (`findFirstByTenantIdAndMessageIdAndEvent` → explicit-boolean → save/publish/flag) — **sequentially idempotent but NOT concurrent-safe**: there is NO ledger-insert-FIRST under a unique index and NO `DuplicateKeyException→Mono.empty()` backstop (unlike H.2's Cal.com path which the plan §9 mandates). Orchestrator judgment: ACCEPTABLE TO PROCEED because (a) it mirrors the *existing shipped* Postmark-webhook idempotency posture (in-place extension, E-D7 precedent — re-architecting the shipped webhook's idempotency model is out of H.4 scope), (b) the effects are advisory/non-money/value-idempotent (a duplicate EmailEngagement row + a duplicate EMAIL_BOUNCED/EMAIL_SPAM advisory event + setting emailDeliverability to the same enum value — no double-charge/double-create), (c) realistic Postmark re-delivery is sequential-with-backoff (handled), concurrent same-(messageId,event) is rare. **The H.8 Opus validator must explicitly rule whether the in-place posture is adequate or a ledger-insert-FIRST backstop is required for the bounce path; this flag is the authoritative escalation, superseding the agent's self-assessment.** H.5 (Activepieces, independent of the Postmark seam) does not compound this; proceeding is safe.
- **H.5 ORCHESTRATOR-VERIFIED:** WebhookDeliveryService + WebhookSubscription entity EMPTY-DIFF (programmatic); exactly ONE additive repo finder (findFirstByTenantIdAndUrlAndEventTypesContaining — verified additive-only, no existing method changed); seed idempotency = explicit-boolean exists-probe read in code (NOT switchIfEmpty(create)); R8 read-only capability token in Javadoc+code (no admin-scope token stored anywhere); @IdempotentRoute + @ConditionalOnProperty(kmosf.modules.activepieces, matchIfMissing=true) report-asserted + Config-Javadoc-corroborated (H.8 validator line-confirms); scope exact/additive; compile green. SHA 6b6a53c. No deviations beyond the one sanctioned additive finder.
- **H.6 ORCHESTRATOR-VERIFIED (deep auth-tier read, real paths):** A2 identity core (JwtTenantResolver/ZitadelClaimTenantResolver/ZitadelOrgTenantCache) + MagicLinkService EMPTY-DIFF (programmatic); PortalSecurityConfig additive-only (OAuthRegistrationsSummary 2→3 params + anyEnabled(); SecurityWebFilterChain bean byte-identical — read); PortalOAuthAuthenticationSuccessHandler +20 = purely additive `registrationId=="zitadel"` early-return inserted after the existing type-check, BEFORE the unchanged Google/Microsoft state-decode path (Google/MS byte-identical — read); OAuthClientRegistrationConfig +46 additive (zitadel registration only when client-id+secret non-blank; https://zitadel.internal.invalid non-routable default); PortalProperties +2 additive record param; application.properties +7 empty-default env refs — no live Zitadel host/secret/registration in any default/test run (read; §7 satisfied). PortalZitadelSuccessHandler (new): reuses A2 ZitadelClaimTenantResolver/JIT VERBATIM via a Jwt-from-OidcUser adapter; opt-in gate tenant.zitadelOrgId==null → 3930 BEFORE any session mint (+ ZitadelOrgTenantCache 3301 primary gate); fail-closed on missing email (3931, no session); switchIfEmpty all genuine not-found (3930/3931 + pre-existing unchanged 1232). NON-OPTED local magic-link/passkey path BYTE-IDENTICAL (§9 #4 auth-regression headline satisfied). compile BUILD SUCCESSFUL true exit 0. SHA 49df0e6. **H.8 VALIDATOR-FOCUS POINTER (not a deviation — fail-closed):** confirm PortalZitadelSuccessHandler.buildJwtFromOidcUser passes the OIDC claims to the unchanged ZitadelClaimTenantResolver without dropping/renaming urn:zitadel:iam:org:id / roles / email — a mismatch fails CLOSED (3300/3301 reject, NOT cross-tenant), a correctness concern for the deep validator read, not a security-bypass.
- **H.7 ORCHESTRATOR-VERIFIED (tests-only proof chunk — G.6 tier):** PRODUCTION-UNTOUCHED invariant HOLDS — `git diff 49df0e6..HEAD -- src/main/` = EMPTY (programmatic; H.7 is src/test-only, H.1–H.6 production byte-stable since the H.6 feat). Scope = exactly 5 new IT classes (CalComWebhookIT/ImapInboundPollIT/PostmarkBounceIT/ActivepiecesSeedIT/PortalZitadelFederationIT, +1454, src/test only — nothing stray). Shard-safe: ZERO functional @MockBean/@MockitoBean (all 5 grep hits Javadoc-only); application-test.properties + build.gradle EMPTY-diff (shard-neutral). Pass-counts (agent-reported, corroborated by production-untouched + forced-clean re-run; AUTHORITATIVE full-suite no-regression = the H.8 sharded CI per §6): 19/0/0/0 across the 5 ITs (CalCom 5, Imap 3, PostmarkBounce 5, Activepieces 3, PortalZitadel 3); integration pkg 25/0/0, portal pkg 77/0/0. SHA def14f9. **⚠ ORCHESTRATOR FLAG #3 (H.8 validator MUST adjudicate):** ImapInboundPollIT exercises the UNCHANGED `InboundEmailService.ingest()` + asserts the poller bean wires — it does NOT drive the H.3 net-new `ImapInboundPoller` fetch/parse/Message-ID-dedupe loop end-to-end, because the poller news-up its jakarta.mail Store internally (not via DI) so a stub Store cannot intercept without a production testability seam (correctly NOT added in the tests-only chunk — flagged not fixed). The H.3 dedupe was verified by code-read; the validator must rule whether a testability seam + a true IMAP-fetch IT is required or code-read+ingest-IT+wiring-assertion is adequate for a default-OFF poller. **⚠ ORCHESTRATOR FLAG #4 (H.8 validator MUST adjudicate):** PortalZitadelFederationIT drives the Bearer-JWT A2 resolver path (mirroring A2's existing harness) + the non-opted-local-still-works + the ZitadelOrgTenantCache 3301 reject — it does NOT drive the H.6 net-new `PortalZitadelSuccessHandler.onAuthenticationSuccess` (the 3930 opt-in gate, buildJwtFromOidcUser adapter, cookie mint) through an actual oauth2Login redirect (needs a fuller mock-OIDC-redirect harness than A2's Bearer-JWT IT). H.6 success-handler internals were verified by deep code-read; the validator must rule whether a redirect-level IT is required. Neither flag is a defect or §9 violation; both are honest test-depth limits on the two hardest-to-drive net-new seams, escalated (not fixed mid-loop — committed H.3/H.6 not bulldozed) for the dedicated Opus validator + the sharded-CI scale gate.

## Validator (Opus 4.7, separate agent) sign-off — external-ingress trust + auth-regression focus — **OVERALL PASS-WITH-NOTES (2026-05-18, real diff 24f7e5e..HEAD, PR #62, ZERO merge-blockers; cleared once sharded CI green — CI subsequently ALL-GREEN run 26067872476)**
BE [x] — switchIfEmpty grep each new integration/* (genuine not-found/2510 only): [x] zero switchIfEmpty(process/create/ingest/reconcile/provision); only genuine 3901/1702/3930/3931 + Javadoc mentions ·
verify-before-effect + tenant-from-URL-path + ledger-insert-first + unique idx + concurrent DuplicateKey→empty (Cal.com/Postmark): [x] Cal.com confirmed (Postmark-bounce probe-then-act — see FLAG 1) ·
reused cores empty/additive diff (the list above): [x] all 13 cores 0-line diff (programmatic) ·
Cal.com source-of-truth: Meeting projection additive, MeetingCrudService/BookingService empty diff, no double-emit: [x] verified ·
IMAP default-OFF + Message-ID idempotent + InboundEmailService empty diff: [x] verified ·
Activepieces seed explicit-boolean idempotent + WebhookDeliveryService empty diff + R8 read-only token: [x] verified (no admin-scope token anywhere) ·
portal Zitadel opt-in; non-opted local path byte-identical; A2 reused verbatim; MagicLinkService.buildLink unchanged; portal/MagicLink/PortalChain ITs green: [x] verified ·
no live external (WireMock/stub/.invalid, default-OFF, no host/token hardcoded): [x] verified ·
shard-safe (no @MockBean, no extra prop source, self-clean); new-resource-client lifecycle (none / shared builder / per-cycle close): [x] verified ·
docs/openapi in-PR; webhook controllers no @ConditionalOnProperty; ActivityType unchanged: [x] verified
FLAG ADJUDICATION (separate Opus, on the real diff — supersedes any agent self-assessment):
- FLAG #1 H.4 idempotency-posture: **PASS-adequate** — in-place extension mirrors the SHIPPED Postmark webhook's own probe-then-save posture (plan H-D4/§9 mandate ledger-insert-FIRST only for NEW ingress = Cal.com); effects advisory/non-money/value-idempotent; sequential path IT-proven (duplicateBounce_idempotent200NoOp_zeroSecondEngagementRow). NOT a blocker. NOTE: residual concurrent-duplicate window (one duplicate advisory row/event, no integrity loss) is a tracked known limit; a future unique (tenantId,messageId,event) index would close it but touches a shipped collection — correctly out of Phase-H scope.
- FLAG #2 H.6 buildJwtFromOidcUser: **PASS fail-closed** — copies ALL OIDC attributes verbatim (c.putAll(oidcUser.getAttributes())); unchanged resolver/RoleClaimMapper read org/roles/email BY NAME; every mismatch fails CLOSED (3300/3301/3302/3930/3931, no session) — no wrong-tenant/bypass.
- FLAG #3 H.7 ImapInboundPollIT coverage: **PASS-adequate** — poller default-OFF (never runs in CI), dedupe is a trivial explicit-boolean probe identical to IT-proven seams, InboundEmailService empty-diff; a production DI seam purely for an OFF poller's IT is disproportionate. NOTE: dedupe loop has no executable proof (acceptable).
- FLAG #4 H.7 PortalZitadelFederationIT coverage: **PASS-adequate** — Bearer path exercises all reused identity components (all empty-diff); non-opted byte-identical invariant IS IT-proven; "no new mock-OIDC harness" was an explicit plan mandate. NOTE: 3930 gate + cookie mint no redirect-level IT (acceptable; deep-code-read verified).
SIGN-OFF (verbatim): "Cleared to merge once sharded CI is green. All four escalated flags rule PASS-adequate, all §9/§10 items PASS. No cross-tenant/replay hole, no Stripe/Cal.com-before-gate, no non-empty reused-core behavior change, no switchIfEmpty(process/create), no live key/host, no @MockBean splinter, no build.gradle shard change, no auth-regression on the non-opted path. The four NOTE-level items are honest test-depth limits on default-OFF/advisory/code-read-verified seams, not defects." → sharded CI run 26067872476 = ALL-GREEN (4 shards + openapi + ci-gate) ⇒ merged.

## Orchestrator finish state — recorded (crash-recovery)
- **H.8 docs ORCHESTRATOR-VERIFIED + BE PR #62 OPEN.** docs-only (git diff 0be585f..HEAD -- src/ EMPTY — production+tests frozen); scope = CLAUDE.md Phase-H SHIPPED note (line 91, accurate) + regenerated docs/api/openapi.json (carries `/public/integrations/calcom/{tenantId}/webhook` + `/api/v1/integrations/activepieces/seed`); `.claude/*` NOT committed; tree clean. SHAs e11596f→586d3f3(wip)→d0f2082→c8e8e32. **PR #62** (https://github.com/kumouri/kmo-digipres-be/pull/62) opened base main@24f7e5e from c8e8e32 (29 commits, src/main +2179/-5 over 33 files). **PENDING two merge gates:** (1) the SEPARATE Opus 4.7 validation on the real diff 24f7e5e..HEAD — MUST adjudicate the four escalated orchestrator flags (#1 H.4 idempotency-posture, #2 H.6 buildJwtFromOidcUser, #3 H.7 IMAP-poller IT-coverage seam, #4 H.7 portal-Zitadel redirect-IT coverage) + the full §9 external-ingress-trust + auth-regression checklist + this Validator sign-off section; (2) sharded CI green (4 shards + openapi + ci-gate — the §6 authoritative full-suite gate). On BOTH green → `gh pr merge --merge` (never squash) → fill the PHASE H COMPLETE line (date + PR#62 + merge SHA + validator sign-off) → /sync-claude-md → PAUSE at the Phase I defer-gate (do NOT start I).

## PHASE H COMPLETE — 2026-05-18. BE PR #62 merge-commit 09f4056. main carries the external-integrations-glue surface
(Cal.com source-of-truth projection; IMAP poll-source default-OFF; Postmark bounce in-place; Activepieces webhook-subscription seed; opt-in portal Zitadel; all WireMock/sandbox — no live external). **Track-1 loop PAUSES here: Phase I is deferred (ultraplan D13) until the first paying client is funded — report, do not start.**
